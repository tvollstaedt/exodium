# Decisions

## 2026-08-14 - LP-Matching family-scoped, Bundled-DB v7
- Entscheidung: Jeder LP↔EN-Match (Shortcode, thumbnail_key, dosbox_conf, has_thumbnail) läuft über `same_group`/`family_expr`; `refresh_catalog` ruft die Verknüpfung nach dem Row-Copy selbst auf.
- Verworfen: thumbnail_key-Kopie in setup.rs behalten - zweite Kopie der Regel driftete bereits (unscoped), gelöscht zugunsten `propagate_lp_thumbnail_keys`.
- Grund: Titel UND Shortcodes wiederholen sich über Pack-Familien (GK2-DE bekam den eXoWin9x-Key, Cover 404te); der Katalog-Refresh stellte die kaputten Keys aus der Bundled-DB bei jedem Start wieder her.
- Gotcha: Die Bundled-DB stempelt IHRE eigene Version; solange sie älter ist als CATALOG_VERSION, läuft der Refresh bei jedem Start - Heilungen müssen also im Refresh selbst passieren, nicht nur in migrate().

## 2026-08-19 - Bibliothek folgt der Absicht, nicht der Platte
- Entscheidung: Der automatische Scan darf Bibliothekseintraege nur noch BESTAETIGEN; `adopt_from_disk` (Import, Rescan-Knopf, Datenverzeichniswechsel) ist der einzige Weg, aus einem Archiv einen Eintrag zu machen. Dazu `collateral_file_indices`: Dateien, deren saemtliche Pieces von angeforderten Dateien belegt sind, fliegen aus der Bibliothek.
- Verworfen: ZIP-Integritaetspruefung und Groessenschwelle als Erkennung - Kollateral ist vollstaendig und valide (8 MiB Piece, eXoDOS-Archive meist kleiner), also nicht am Inhalt erkennbar.
- Grund: Zwei Nutzerreports (4 -> 17 Spiele nach einem Download; vier ZZT-Titel nach util.zip). Pass 2 zaehlte jedes Archiv >= 1 KB als Installation.
- Gotcha: Kollateral-Bereinigung und Adoption sind Gegensaetze und laufen nie zusammen - sonst entfernt der Scan Zeilen, die er eine Zeile spaeter wieder anlegt. Die Sollgroesse kommt aus dem gebuendelten .torrent, nicht aus `download_size` (das ist ZIP + GameData).

## 2026-08-19 - Engine-Wahl zentral, ECE-Spiele optional unter Staging
- Entscheidung: `resolve_engine` ist die einzige Stelle, die ECE gegen Staging entscheidet (`launch_game`, `game_uses_ece`, `game_printing_unavailable`, Panel-Label). Pro-Spiel-Key `engine = staging` erzwingt Staging; der Settings-Dialog zeigt das Feld nur, wenn ECE ueberhaupt laufen wuerde, und begruendet den deaktivierten Shader-Schalter daneben.
- Verworfen: globaler Engine-Schalter (stellt unter Windows 2000+ Spiele auf eine ungetestete Engine um), automatisches Umschalten sobald CRT an ist (Emulationswechsel als unsichtbare Nebenwirkung einer Optik-Einstellung), Shader-Feld ausblenden (erklaert nichts).
- Grund: Nutzer stellte global UND pro Spiel "on" und suchte die Config-Datei, in der der Wert haengt. Bei ECE wird er nicht ueberschrieben, sondern verworfen - ECE hat keine Shader-Pipeline (von 750 eXo-Confs mit `glshader` gehoeren 675 zu Staging-Varianten, 16 zu ece; ECE-Spiele bekommen `output=openglnb` plus Scaler).
- Gotcha: `game_engine_info` liefert ZWEI Antworten - `ece_available` (gibt es die Wahl?) und `uses_ece` (was laeuft wirklich?). Der Dialog fragt die erste: mit der zweiten verschwand die Auswahl, sobald jemand Staging waehlte, und der Weg zurueck war weg.
- Gotcha: Die Engine-Antwort haengt an Plattform, extrahiertem ECE-Build UND Override - das Frontend kann sie nicht ableiten, es fragt. Die Druckernotiz kippt mit dem Override auf "nicht verfuegbar", was korrekt ist: Drucken kann nur ECE.

## 2026-08-25 - Substituierte Host-Pfade: kein Trailing Separator, Quoting nur fuer mount
- Entscheidung: `to_working_dir` liefert nie einen Pfad mit abschliessendem `/`, und `rewrite_host_paths` setzt Anfuehrungszeichen um eine Ersetzung mit Leerzeichen - aber nur, wenn die Zeile ein `mount`/`imgmount` ist (`on_mount_line`).
- Verworfen: unter Windows native Backslashes ausgeben. Das verwischt die Grenze Host-Pfad/Guest-DOS-Text wieder, die den kaputten Guest-`PATH` gekostet hat (1.122 Win3x-Spiele).
- Grund: DOSBox entfernt vor dem `stat()` des Mount-Ziels nur einen abschliessenden BACKSLASH (Windows-Workaround); unser `/` rutscht durch, der Mount scheitert, das Autoexec laeuft in sein `exit`. Betrifft 1.570 eXoDOS-Confs (u.a. DOOM II) und 91 eXoWin3x-Confs, nur unter Windows - POSIX-`stat` akzeptiert den Trailing Slash, deshalb auf dem Dev-Rechner unsichtbar.
- Gotcha: Quoting gehoert NUR an Kommandoargumente. Ein Config-Property (`fluid.soundfont=`) liest seinen Wert woertlich, dort wuerden die Anfuehrungszeichen Teil des Pfades.

## 2026-08-30 - Ein Tracker-Objekt pro Download statt zwoelf paralleler Tabellen (#22)
- Entscheidung: `stores/downloads.ts` haelt den Zustand eines Downloads in einer `Map<number, Tracker>`; die Poll-Schleife plant sich selbst (`while (!t.cancelled) { await sleep; await tick }`) statt per `setInterval`. Beenden heisst `endTracker`, und das prueft die Identitaet (`trackers.get(id) === t`), damit ein auslaufender Lauf nicht den Versuch abmeldet, der ihn ersetzt hat.
- Verworfen: `@tanstack/solid-query` - absorbiert nur den Scheduler. Zwei-Signal-Stall-Erkennung, Extras-Phase nach dem Install, Torrent-Validierungsfortschritt und Restart-Resume blieben Domaenenlogik, die zwoelf Tabellen also stehen, plus neue Abhaengigkeit.
- Grund: 66 `delete …[gameId]` ueber sechs Exit-Pfade; ein vergessenes leckt Zustand in den naechsten Versuch. Genau so entstand 9c32855e (Cancel auf einen stockenden Download liess die Karte auf ihrem letzten Prozentwert stehen).
- Gotcha: Der Generationszaehler ist weg, aber die Regel nicht - jeder `await` in `tick` muss danach `t.cancelled` lesen. Und der 5s-Timer nach dem Install prueft jetzt `trackers.has(id)`, sonst loescht er den Eintrag eines Downloads, der in diesem Fenster neu gestartet wurde.

## 2026-08-30 - Titelmusik aus dem GameData-Archiv: ein Job-Kern fuer Video und Musik
- Entscheidung: `media.rs` ist ueber `MediaKind {Video, Music}` parametrisiert (Finder, Cache-Verzeichnis, Marker, Endung); `MusicState` ist eine zweite Instanz derselben Job-Map. Katalog-Hint `music_file TEXT` (Basename aus MusicPath, sonst `<Titel>.mp3` bei MissingMusic=false), CATALOG_VERSION 11. Shuffle immer ueber die ganze eXoDOS-Familie, genau ein Track Prefetch, nach 60 s still ueberspringen. Der Frontend-Cap von 3 Streams ist fuer Videos und Musik gemeinsam (`stores/mediaQueue.ts`, Prio Vordergrund-Video > gewuenschter Track > Prefetch/Hintergrund).
- Verworfen: paralleler Musik-Pfad (~250 Zeilen Kopie mit zwei Timeout-Vertraegen); `has_music` + Endung als zwei Spalten; ein Backfill-Helfer wie `populate_manual_paths` (der Refresh kopiert neue Spalten selbst); Shuffle ueber den aktuellen Grid-Filter.
- Grund: Video und Theme liegen im selben Zip unter `Music/MS-DOS/`; bei Archiven bis 8 MiB bringt der Video-Fetch das Musik-Piece schon mit. `MissingMusic=false` ist LaunchBox-Default, kein Inventar - Win3x sagt es 1.120-mal und hat keine Datei; darum nullt `generate_db` den Hint ohne `gamedata_torrent_index`, und ein `.nomusic`-Marker faengt den Rest ab.
- Gotcha: Die Musik-Cachedatei behaelt ihre echte Endung (ServeFile leitet den Content-Type daraus ab, GStreamer den Demuxer) - der Cache-Lookup geht deshalb ueber den Stamm. Der Shuffle startet nie beim App-Start.
- Gotcha: Die Pausegruende sind pegelgesteuert - `pauseFor` merkt sich einen Grund immer, auch wenn gerade nichts lief, sonst spielt ein SPAETER geladener Track ueber laufendes Spiel oder Trailer hinweg; dass ein still gestartetes Spiel beim Beenden keine Musik anwirft, macht `userPaused`, nicht die Grundmenge. Und ein "none" mit gesetztem `error` (OFFLINE_TOKEN) ist vorlaeufig: weder `music.ts` noch `videos.ts` duerfen es als "hat keine Musik/kein Video" cachen, sonst ist das Spiel nach einem Offline-Besuch fuer die Session gesperrt.

## 2026-08-30 - Browse ist die Jukebox: kein Music-Tab, keine Musik-Collection
- Entscheidung: Filter-Chip "With theme" (`with_music`) in der Browse-Toolbar, ▶ nur auf `GameRow` (Listenmodus), Chip schaltet den Modus fuer die Session um ohne `view_mode` zu schreiben. Die sichtbare Liste ist die Queue (`music.ts` Modus `list`): ⏭ folgt der aktuellen Sortierung, am Seitenende `fetchMoreGames`, am Listenende Stopp. `music_cache_index` liefert gecachte und als "kein Theme" bekannte IDs, `put()` haelt die Mengen live.
- Verworfen: dritter Tab (zweite Liste ueber dieselben Zeilen, eigene Suche/Sortierung/Paging); Pseudo-Collection (erbt jede `COLLECTION_MAP`-Projektion: Pack-Settings, Download-Buttons, `enable_new_collections`); ▶ als Hover-Overlay auf der Grid-Karte.
- Grund: Jeder Track gehoert zu einem Spiel (Stage A per Archiv, Stage B nach eXo-Konvention titelbasiert). Genre/Suche/Playlists werden so gratis zu Musikfiltern.
- Gotcha: `with_music` ist ein EIGENES EXISTS ueber die Gruppe, keine Varianten-Bedingung - der Hint sitzt auf der EN-Zeile, der Collection-Filter trifft die LP-Zeile; in einem EXISTS zusammen war der GLP-Shelf mit Chip leer. Cache-Stems am LETZTEN `_` trennen (`eXoDOS_GLP_3.ogg`). `playable()` lehnt jetzt alles ausser mp3/ogg ab, Tracker-Hints erreichen den Shuffle nicht mehr. Im Theme-Modus zeigt die Bar statt ⏮/⏭ einen expliziten Shuffle-Button - ohne Queue versprachen die Skip-Buttons ein Album, und ⏭ startete unsichtbar den Shuffle. Das × der Bar blendet aus + pausiert (User-Pause, nichts resumed automatisch); der ♪-Top-Bar-Button holt sie zurück bzw. startet ohne Track den Shuffle. `stop()` bleibt der interne Voll-Reset.

## 2026-09-06 - eXoScummVM: Launch ueber eXos Index, Pins als sieben Builds, Varianten im Zip
- Entscheidung: `CollectionDef.launcher` (DosBox/Win9x/ScummVm) ist der Dispatch-Schalter; `commands/scummvm.rs` baut die Kommandozeile aus dem gebuendelten `scummvm.txt` (dir -> engine:gameid -> Build) und dem Variantenbaum im Spielordner, laeuft `--detect` vor jedem Start, Saves nach `<Spielordner>/!saves`. Pro Pin ein Build: Windows aus `utilSVM.zip`, macOS/Linux als Content-Pack `scummvm-<version>`, Fallback PATH/Flatpak mit Hinweis im Panel.
- Verworfen: ein ScummVM fuer alle (Thomas: eXo pinnt nicht ohne Grund, Anspruch "on par mit eXoDOS"); Varianten als Katalogzeilen (833 -> 1.500+, mehrere Karten pro Spiel, Baum erst nach dem Entpacken bekannt); ScummVM als Tauri-`externalBin` (sieben Builds mal 200 MB); Saves im globalen ScummVM-Ordner (Uninstall-Backup verfehlt sie).
- Grund: Kein per-game Conf - eXo selbst hat eine Bat plus Index, die ganze §10a-Mount-Logik entfaellt. Der Root-`scummvm.exe` ist 2.8.0 (die Ini behauptet 2.6.1), die drei Git-Snapshots tragen ihren Commit im Versionsstring, also reproduzierbar.
- Gotcha: Falsche Spieldaten enden NICHT mit Exit != 0, sondern im ScummVM-Launcher-GUI - deshalb der `--detect`-Vorlauf. `--no-console` gibt es nur unter Windows. `Command::output()` braucht auf macOS denselben fork+exec-Trick wie der Emulator-Spawn (posix_spawn EBADF). Der XML-Merge muss alle `<Game>` vor die `<AlternateName>` stellen, sonst "duplicate field `Game`" im Importer.
- Gotcha: `games.platform` bleibt `ScummVM SVN` fuer eXos instabile 109 Titel - eine Collection, zwei Thumbnail-Laeufe. Drei Zips weichen vom XML nur in Gross-/Kleinschreibung ab; `gen_scummvm_assets.py` schreibt die Katalogpfade auf die Zip-Schreibweise um, weil der Shortcode exakt gegen den entpackten Ordner verglichen wird.

## 2026-09-06 - Support-Payloads: ein `SupportPack` pro util-Zip statt einer Pipeline pro Collection
- Entscheidung: `support_files.rs` traegt Extraktion, Watcher, Queue und Rearm einmal; `DOS_SUPPORT` und `WIN9X_SUPPORT` sind statische Datensaetze (Suffix, inneres Zip, Prefixe, Readiness, Post-Hook), `PACKS` ist die Liste, ueber die `init_download_manager` rearmt. Rename-Einheit ist der Prefix in seiner echten Schreibweise (`staged_dir`), Running/Failed-Flags haengen am Pack.
- Verworfen: Trait mit zwei Impl-Structs (Zeremonie fuer zwei Konstanten-Saetze); die DOS-Pipeline auf Windows unangetastet lassen (waere die dritte Kopie mit ScummVMs `utilSVM.zip`).
- Grund: Beide Pipelines waren bis auf fuenf Konstanten identisch, ScummVM Phase 2 braucht eine dritte.
- Gotcha: Windows-ECE-Extraktion nur ueber den synthetischen Test abgedeckt, Feldtest in der Windows-VM steht aus. Prefixe sind lowercase, weil der Vergleich lowercase ist - `emulators/86box98/` findet `emulators/86Box98/`.

## 2026-09-06 - Launch-Vorstufe einmal: `extract_before_launch` fuer alle drei Launcher, typisierte Zip-Fehler
- Entscheidung: `ExtractError { NotAnArchive, Other }` entsteht dort, wo das Zip geoeffnet wird (`From<ZipError>`: InvalidArchive/UnsupportedArchive = Placeholder oder Fragment); `LaunchError` traegt die Ausgaenge der Vorstufe und wird erst an der Command-Grenze zum String. `launch_zip_candidates` kennt beide Zip-Orte (Collection-Layout, Prefix-Root), damit DOS, Win9x und ScummVM denselben Pfad nehmen.
- Verworfen: `CommandError` ueber alle 278 `map_err` (zu breit, kein Aufrufer matcht ausserhalb der Launch-Pfade); `thiserror` fuer zwei Enums.
- Grund: Vier Stellen matchten auf `contains("EOCD")`, drei Bloecke unterschieden sich nur in der Kandidatenliste.
- Gotcha: `reset_game_data` bleibt String-typisiert (`map_err(String::from)`), weil seine Closure eigene Fehlertexte vor dem Entpacken baut.

## 2026-09-06 - Panel-Hinweise als reine Funktion, Win9x-Sonden als Primitive
- Entscheidung: `launchNote(ctx)` in `src/launchNotes.ts` entscheidet aus einem Snapshot (Spiel, Plattform, Engine-Antworten, Payload-Status, Pack, Job) welcher EINE Hinweis ueber der Aktionsleiste steht; `emulatorName`, `isWin9x`, `isScummVm`, `emulatorPackId` liegen daneben. `createWin9xStatus(game, settled)` in `components/win9xStatus.ts` buendelt Engine-Sonde, Multiplayer-Info und Payload-Polling inkl. Re-Probe nach Pack-Install. Das Panel baut nur noch den Kontext und rendert.
- Verworfen: Hinweise im Backend berechnen (Texte sind UI, die Buttons braeuchten die Signale trotzdem); ein globaler Store fuer den Win9x-Status (die Sonden gehoeren zum offenen Panel, nicht zur App).
- Grund: 200 Zeilen if-Kette an zehn Signalen waren nicht testbar; jetzt 15 Tests ueber die Prioritaeten.
- Gotcha: Der Kontext ist ein Snapshot pro Aufruf, Solid trackt die gelesenen Signale trotzdem, weil `rawNote` innerhalb eines Effekts/JSX aufgerufen wird. `installPack` bekommt die Collection aus dem Panel, weil der Pack-Job-Key `<collection>:<pack>` heisst.

## 2026-09-06 - commands/ nach Verantwortung geschnitten statt nach Entstehungsgeschichte
- Entscheidung: games.rs (4.3k) und setup.rs (4.3k) in `collections`, `paths`, `layout`, `library`, `install`, `assets` aufgeteilt; games.rs behaelt Getter, Settings und die DOSBox-Launch-Pipeline, setup.rs den Wizard, `init_download_manager` und `factory_reset`. Reines Verschieben, ein Commit pro Zielmodul, Tests wandern mit ihrer Funktion. `mod.rs` re-exportiert weiter, lib.rs und die Integrationstests sehen keine Aenderung.
- Verworfen: `launch.rs` zusaetzlich herausloesen (games.rs waere dann ~1.100 Zeilen; kommt, wenn die Launch-Pipeline das naechste Mal angefasst wird); Re-Exports der alten Pfade in games.rs/setup.rs belassen (verdeckte Indirektion, genau das Muster, das den Split noetig machte).
- Grund: Beide Dateien waren Sammelbecken ohne Modulgrenze; jede Aenderung an Download oder Scan brauchte eine Suche durch 4k Zeilen.
- Gotcha: Vier Helfer wurden dafuer `pub(crate)` (`game_op_lock`, `running_games`, `running_game_key`, `copy_dir_recursive`, `extract_game_zip`); sie sind die geteilte Basis von Launch, Install und Library. `prune_gallery_cache_at_startup` ist der eine Einstieg in den Gallery-Cache von aussen.

## 2026-09-06 - Kommentar-Pass: Vertrag statt Geschichte
- Entscheidung: Kommentare beschreiben, was gilt (Vertrag, Invariante, der eine nicht-offensichtliche Grund), maximal vier Zeilen pro Funktions-Doc; Messwerte, Vorfaelle und verworfene Alternativen stehen in DECISIONS.md und den CLAUDE.md-Paragraphen, im Code nur als `(§N)`-Verweis. Regel steht jetzt in den Conventions der CLAUDE.md.
- Verworfen: Kommentare ganz streichen (die Invarianten sind das Wissen, das im Code nicht steht); Historie in `git blame` verweisen (die Erklaerung, WARUM eine Zeile so ist, bleibt im Code, nur die Anekdote geht).
- Grund: 2.353 Zeilen in Bloecken ab fuenf Zeilen, zu grossen Teilen Erzaehlung und Wiederholung der CLAUDE.md; jetzt 119. Netto minus 1.947 Zeilen, kein Code geaendert.
- Gotcha: Die Paragraphen-Nummern der CLAUDE.md sind damit Referenzziele - beim Umnummerieren `grep "§"` ueber src/ und src-tauri/.

## 2026-09-06 - zip64 im Range-Reader (Blocker fuer den Media-Pack-Spike, #27)
- Entscheidung: `zip_range::read_central_directory` folgt bei den 0xFFFF-Sentinels dem zip64-Locator (20 Bytes vor dem EOCD) zum zip64-EOCD-Record und liest Eintragszahl, Verzeichnisgroesse und -offset als u64; pro Eintrag ersetzt das Extra-Feld 0x0001 die uebergelaufenen Felder in Spec-Reihenfolge. Verzeichnisse ueber 64 MB werden abgelehnt (`MAX_DIRECTORY_BYTES`), der 512-MB-Deckel pro Eintrag bleibt.
- Verworfen: die `zip`-Crate fuer das Verzeichnis (synchron, liest ueber den Torrent-Stream ganze Pieces); den Tail groesser lesen statt ein zweites Mal zu seeken (der Record muss nicht im Tail liegen).
- Grund: `DOSSoundtracks.zip` hat 36 GB und ist damit zwingend zip64; der Reader brach mit "zip64 archive not supported" ab.
- Gotcha: Der Local Header wird weiter nur uebersprungen (Name + Extra per Laenge), dort braucht es kein zip64. Test-Fixtures sind handgebaut, weil der `zip`-Crate zip64 nur bei Bedarf schreibt.

## 2026-09-06 - Media-Pack-Spike: DOSSoundtracks.zip ist eine LaunchBox-Plattform aus gespeicherten Album-Zips
- Entscheidung: `zip_range::OffsetReader` (Fenster auf einen STORED-Eintrag) und `entry_data_offset` sind pub, damit ein Album-Zip im 39-GB-Archiv mit demselben Tail-first-Parser gelesen wird. `examples/media_pack_spike.rs` ist das Werkzeug dafuer (Torrent mit leerer Auswahl, gestreamtes Verzeichnis, optional EXTRACT/NESTED). Die Media-Pack-`.torrent` bleibt vorerst ausserhalb des Repos.
- Verworfen: die Album-Zips als Ganzes laden (Median ~150 MB, bis 3 GB - genau der Bulk-Download, den #14 ausschliesst).
- Grund: Gemessen: 584 Eintraege, 126 Album-Zips unter `eXo/Soundtracks/`, alle STORED; 453 Bilder unter `Images/Soundtracks/<Kategorie>/`; `Data/Platforms/Soundtracks.xml` (0,55 MB) beschreibt die Alben als LaunchBox-"Games" (Titel, Komponist als Developer, Notes, CommandLine = Album-Zip). Aeusseres Verzeichnis in 3-4 s, Album-Verzeichnis in 11 s, erster Track dann in 0,1 s; ein Album kostet ~56 MB auf der Platte. Titel-Match Album -> eXoDOS-Spiel 54 von 126 exakt, der Rest braucht einen Index von Hand (Compilations, OST-Suffixe, Serien).
- Gotcha: Ohne den leeren Fastresume-Bitv hash-checkt librqbit 220 GB Platzhalter, bevor `stream_file` moeglich ist ("invalid state: initializing"); das Beispiel seedet ihn wie die App. Die XML hat keine Verknuepfung zum Spiel - das Mapping muss in `generate_db` entstehen.

## 2026-09-06 - ScummVM Phase 3: Plattform-Chips im Panel, Menues im Settings-Dialog
- Entscheidung: `scummvm_variants` liefert den Baum (`list_variants`) plus die Auswahl, die ein Start nehmen wuerde; `set_scummvm_options` nimmt IMMER den vollen Satz (variant, sub, sound, subtitles, aspect) und loescht fehlende Keys. Panel: ein Chip pro Plattform-Ordner, Label = Klammerinhalt ("DOS v1"), Klick schreibt `svm_variant` und setzt nur `svm_sub` zurueck. Dialog: erkennt ScummVM am nicht-null Baum (kein neues Prop) und zeigt Edition/Sound/Untertitel/Seitenverhaeltnis statt der DOSBox-Controls; Fullscreen bleibt. `note.txt` laeuft als dismissbarer Hinweis `svm-note` durch `launchNote`, zwischen "Engine fehlt" und "System-ScummVM".
- Verworfen: Teil-Updates mit "fehlt = unveraendert" (haette Option<Option<..>> in serde gebraucht, und der Dialog hat den vollen Satz ohnehin); Variante auch im Dialog (die Plattform ist die Entscheidung vor dem Play, nicht eine Einstellung).
- Grund: Der Baum ist erst nach dem Entpacken bekannt, deshalb null fuer nicht installierte Spiele und Nachladen nach dem Install-Uebergang im Panel.
- Gotcha: Sound-Menue haengt von der Unter-Variante ab (`probe` prueft run_dir vor dir1): die eigene Liste der Edition gewinnt, wenn nicht leer. Verifiziert im Dev-Build mit Maniac Mansion (neun Chips, Wechsel schreibt `svm_variant`); der Dialog nur per Test, der UI-Lauf wurde von einem fremden Fenster unterbrochen.

## 2026-09-06 - ScummVM Phase 2a: Emulator-Packs verdrahtet, Release folgt
- Entscheidung: Vier Release-Packs (2.5.0, 2.8.0, 2.9.0, 2026.1.0) im Manifest mit Platform-Map wie DOSBox-X; Git-Snapshots nutzen das naechstliegende Release (`pack_version`). Auto-Queue in `download_game` ist NICHT resolver-gated wie bei Win9x: nur eXos Build oder das Pack selbst verhindern den Download, ein System-ScummVM nicht, weil er den Pin ignoriert. `SCUMMVM_SUPPORT` als dritter `SupportPack` (utilSVM.zip: mt32/ ueberall, emulators/scmvm/ auf Windows). Panel-Notiz bietet das Pack mit Button an, auch neben einem System-ScummVM.
- Verworfen: Snapshot-Builds jetzt (17 Spiele, brauchen Source-Builds vom Commit in CI - kommt mit 2b); Packs als `available` ohne URL (waere ein Button, der 404 laeuft).
- Grund: Thomas: ScummVM soll die App selbst laden, kein Umweg ueber scummvm.org. Offizielle Binaries gibt es nur fuer macOS (DMG, universal), Linux baut wie DOSBox-X selbst.
- Gotcha: Leere Platform-URLs bedeuten `available: false`, damit zeigt die Notiz bis zum Release den scummvm.org-Text. `pack_id` in `ScummVmEngineInfo` ist auf Windows None. `scummvm_ready` ist nach einem DOS-`util.zip`-Extract auf macOS/Linux schon wahr (gleiche mt32/-ROMs), utilSVM.zip wird dann nicht geladen - gewollt.

## 2026-09-06 - ScummVM Phase 2b: Pack-Build im Workflow, Release noch offen
- Entscheidung: `content-packs.yml` bekommt einen `packs`-Schalter (win9x|scummvm) und je eine Matrix ueber die Pins. macOS packt das offizielle DMG um (universal, gemessen an 2.5.0: x86_64 + arm64 - deshalb zeigen beide Mac-Tokens auf dieselbe Tarball), Linux kompiliert aus dem Release-Tarball wie DOSBox-X. Sparkle bleibt drin und wird per Info.plist stillgelegt (`SUEnableAutomaticChecks`/`SUAutomaticallyUpdate` false), danach ad-hoc neu signiert.
- Verworfen: Sparkle.framework loeschen (`scummvm` linkt es ueber `@rpath`, das Binary startet dann nicht); Arch-Pakete statt Quellbau (haette die Pins aufgegeben, genau das, was §18 verhindert).
- Grund: Upstream liefert fuer Linux nur .deb, Flatpak und Quelltext - kein Binary einer einzelnen Version.
- Gotcha: Der Linux-Zweig ist UNGEPRUEFT (braucht den pkgforge-Container; ein alter Pin von 2021 kann am aktuellen Toolchain scheitern). Der macOS-Zweig wurde lokal fuer 2.5.0 durchlaufen: Wrapper `scummvm-2.5.0/`, Binary am Resolver-Pfad, 6 Symlinks erhalten, keine AppleDouble-Eintraege, Signatur gueltig, `--version` meldet 2.5.0. ScummVM laedt Themes und Engine-Daten zur Laufzeit aus `../share/scummvm`, quick-sharun buendelt nur Bibliotheken - das Datenverzeichnis wird im Skript von Hand kopiert und per Gate geprueft.

## 2026-09-06 - Autoplay ueberstimmt den Hoerer nicht mehr; keine zwei Download-Aktionen nebeneinander
- Entscheidung: Der Theme-Autoplay im Panel startet nur noch, wenn weder `musicUserPaused` noch `playerHidden` gesetzt ist; sonst wird der Track nur geholt (`requestTheme`). `playTheme` selbst bleibt unveraendert die "der Nutzer will das jetzt"-Funktion der ▶-Knoepfe. Und die ScummVM-Notiz ist fuer ein NICHT installiertes Spiel informativ statt blockierend ("Mit diesem Spiel kommt auch ScummVM X, einmalig N MB"), weil dessen Download-Knopf die Engine ohnehin mitzieht - dieselbe Form wie `win9x-support-size`.
- Verworfen: `playTheme` selbst die Absage respektieren lassen (dann koennte das ▶ einer Zeile nach einem × nichts mehr starten); die Notiz beim nicht installierten Spiel ganz weglassen (der Preis des Downloads waere dann unsichtbar).
- Grund: `playTheme` raeumt `userPaused` und `barHidden` ab. Der Autoplay ruft es bei jedem geoeffneten Spiel, also hielt das × nur bis zum naechsten Klick auf eine Karte - gemeldet als "springt immer wieder an, spielt staendig neue Songs". Und der rote Hinweis stand direkt ueber einem Download-Knopf, der dasselbe Problem loest.
- Gotcha: Der zweite Teil des Musik-Reports ist NICHT behoben - ein Theme kann fuer ein bis zwei Sekunden ueber einem startenden Vorschauvideo laufen, weil das Video die Lautsprecher erst in seinem `onPlay` beansprucht (und bei stummem Autoplay-Fallback gar nicht). Offene Entscheidung: Theme bei vorhandenem Video gar nicht erst automatisch starten.

## 2026-09-07 - E2E ueber tauri-driver, nur im VM-Labor, mit vorbelegtem Offline-Zustand
- Entscheidung: `e2e/` mit WebdriverIO 9 gegen `tauri-driver`, gefahren von `vm-lab/jobs/exodium.{sh,ps1}` nach dem Debug-Build (Linux unter Xvfb, Windows in der interaktiven Sitzung per `schtasks /it`). `examples/e2e_seed.rs` kopiert die gebuendelte DB ins echte App-Verzeichnis und schreibt `data_dir`, `root_folder`, `network_mode=offline`, `seeding_enabled`, `welcome_seen`, `collections` - kein Assistent, kein Torrent, kein Dialog ueber dem Raster. Zwoelf `data-testid` als Anker; alle Spezifikationen laufen in EINER Sitzung (`specs: [[...]]`), der Treiber startet einmal in `onPrepare`.
- Verworfen: Selektion ueber CSS-Klassen (Stilhaken, brechen still); GitHub-CI (xvfb- und Treiberfassungen ohne interaktive Untersuchung); eigener WebDriver-Client (Warten/Wiederholen selbst gebaut); den Assistenten per OS-Automation fahren (nativer Dateidialog, pro Plattform anders).
- Grund: macOS hat keinen WebDriver fuer WKWebView, E2E existiert also nur in den VMs. Die beiden Befunde, die das Labor bisher lieferte (Start- und Renderpfad), sind genau die Klasse, die ein Rauchtest ohne Netz abdeckt.
- Gotcha: **Windows: das Hauptfenster entsteht in `setup()` (`create: false` in beiden Konfigs), weil msedgedriver den Debug-Port ueber `WEBVIEW2_ADDITIONAL_BROWSER_ARGUMENTS` anfordert und WebView2 die Variable ignoriert, sobald die App eigene Argumente setzt - wry tut das immer.** Gemessen: `WEBVIEW2_USER_DATA_FOLDER` wirkt, die Argument-Variable nicht (kein `DevToolsActivePort`, Port zu). Nur bei gesetzter Variable haengt die App deren Wert an wrys Standardargumente; sonst ist das Fenster identisch. Der Seeder ueberschreibt die ECHTE Datenbank (Tauri leitet das Verzeichnis aus der Kennung ab) - darum die `EXODIUM_E2E=1`-Sperre. `$$()` in WDIO 9 haelt sein Ergebnis nach dem ersten `await` fest; in `waitUntil` pro Iteration neu abfragen. Eine zweite Sitzung nach `deleteSession` beantwortet tauri-driver nur noch mit hyper-`SendRequest`-Fehlern. Der Klick-Handler der Karte sitzt auf `.game-card-art`, nicht auf der Karte, und die Art hat KEINE Hoehe, bis ihr Cover geladen ist - auf das `<img>` warten, nicht auf die Karte (eine Karte ohne ladbares Cover hat damit auch fuer Nutzer keinen Klickbereich; offen). Nach einer Suche existiert die ALTE erste Karte weiter, bis das Ergebnis kommt - "eine Karte existiert" beweist nichts, auf den Titel warten. Eine Katalogsuche braucht auf dem Windows-Gast 5-10 s (Linux: unter 1 s; die Haelfte davon war Defender auf `%APPDATA%`) - `waitforTimeout` 60 s, und die Latenz auf echter Windows-Hardware ist eine offene Messung. `ring` verlangt auf Windows-ARM64 clang. pnpm 12 lehnt Pakete unter 24 h Alter ab (`minimumReleaseAge`) - die Jobs pinnen 10.27.0.

## 2026-09-07 - Eine Dateilaenge beweist auf einem Torrent-Baum nichts
- Entscheidung: Ueberall, wo eine Groesse als Vollstaendigkeitsbeleg diente, fragt der Code jetzt die Sitzung: der Start-Scan bekommt die `in_flight`-Menge (selected && !complete) und setzt so bestaetigte `installed`-Zeilen zurueck; `extract_before_launch` verweigert ein Archiv, das die Sitzung noch fuellt (`StillDownloading`); der Support-Watcher nutzt die Plattengroesse nur ohne Session-Handle und zaehlt Fehlversuche nur bei bestaetigter Vollstaendigkeit; `support_files::status` meldet verifizierte Bytes; die Layout-Migration entscheidet einen Groessen-Gleichstand danach, welches Archiv sich oeffnen laesst. Downloads ueberleben einen Neustart: `list_active_downloads` + `resumeDownloads` legen die Tracker neu an - der Poll ist es, der am Ende entpackt. Ein `.extracting`-Lock aelter als 5 min gilt als abgestuerzte Extraktion: Zeile zurueck auf `installed = 0`, Lock weg, der Tracker entpackt neu.
- Verworfen: die Groesse durch die belegten Bloecke ersetzen (Windows hat dafuer keine portable API); einen zweiten "wird geladen"-Zustand persistieren (librqbits Auswahl IST dieser Zustand).
- Grund: librqbit legt Dateien sparse an; nach dem ersten Stueck hat die Datei volle Laenge. Gemessen im Labor: halb geladene Spiele im Regal "Installed", Extraktion auf halbe Archive (`EXTWin9x.zip not found inside`, `Invalid checksum`), nach drei Versuchen `failed` bis zum Neustart.
- Gotcha: Support-Praefixe muessen benennen, was das Pack BESITZT - `emulators/dosbox/` haelt eXoDOS' ece-Builds UND eXoWin9x' x98, und ein Praefix auf den Elternordner loeschte die ECE-Builds bei jeder Win9x-Extraktion (Test `a_pack_leaves_its_siblings_under_a_shared_parent_alone`). Ein Praefix darf jetzt auch eine Datei sein. `sweep_leftovers` raeumt `.support_staging_<pid>`/`*.support_tmp_<pid>` fremder Prozesse weg (2,9 GB nach zwei harten Stopps).

## 2026-09-07 - ScummVM: ein Pack fuer die Plattform schlaegt jede Systemkopie
- Entscheidung: `resolve_scummvm` faellt nur dann auf PATH/Flatpak zurueck, wenn das Manifest fuer die laufende Plattform KEIN Pack mit der gepinnten Fassung kennt. Sonst gilt "nicht vorhanden", das Panel nennt die Pack-Groesse, `download_game` holt es mit dem Spiel. Linux-Packs werden jetzt pro Architektur gebaut (`scummvm_arches`, aarch64 auf GitHubs ARM-Runnern, pkgforge-Container ist multi-arch).
- Verworfen: Systemkopie bevorzugen und das Pack nur anbieten (so war es) - im Labor lief prompt ein apt-ScummVM statt des Pins, und der Hinweis "your system's ScummVM will run it instead" ist fuer den Nutzer ein Fehler, kein Angebot.
- Grund: eXo pinnt, weil Spiele ueber Releases brechen; eine Systemkopie ist keine Loesung, sondern der Zustand, den die Packs beenden sollen.
- Gotcha: Ohne Pack fuer die Plattform (heute: alles ausser darwin-*, linux-x86_64, linux-aarch64) bleibt das System der einzige Weg, und der scummvm.org-Hinweis ist dort ehrlich.

## 2026-09-08 - ScummVM: Plattformcodes uebersetzt, mehrteilige Glk-Spiele auf Teil 1 gepinnt
- Entscheidung: `platform.txt` traegt eXos Kuerzel (`sp48`, `st`, `dos`), ScummVM will seine eigenen (`zx`, `atari`, `pc`) und verwirft sonst die ganze Kommandozeile - `scummvm_platform_code` uebersetzt, Unbekanntes laesst `--platform` weg. Liefert `--detect` mehrere Zeilen fuer eine Kennung (Level-9-Trilogien: `GAMEDAT1-3.DAT` in einem Ordner), fragt `pin_first_part` ScummVM pro Datei, welche Teil 1 ist, schreibt ein Target mit `filename=` in die Ini und startet das Target.
- Verworfen: die alphabetisch erste Datei nehmen (Heuristik ueber Dateinamen, die ScummVM selbst besser kennt); `--massadd` (in den Builds nicht einkompiliert).
- Grund: Der nackte Start mit `engine:gameid` nimmt den ersten Treffer der Verzeichnisliste - NTFS alphabetisch (Teil 1, darum laeuft eXos Bat), APFS in Hash-Reihenfolge (Teil 2: "you've started in the middle of the game").
- Gotcha: Gemessen mit dem 2.8.0-Pack auf macOS; die Probe kostet einen `--detect` pro Datendatei, nur wenn es mehrere Treffer gibt.

## 2026-09-08 - eXoScummVM: Poster- und Metadata-Pack
- Entscheidung: `posters-eXoScummVM-v1.tar.gz` (832 Cover, 46 MB) auf `content-v7` nachgeschoben - dasselbe Release wie die Emulatoren, bestehende Assets unangetastet; Metadata als Torrent-Pack (`Content/XOScummVMMetadata.zip`, 4,2 GB) ohne Upload. Der Galerie-Scan nimmt jeden `Images/<platform>`-Ordner mit dem Plattform-Praefix der Sammlung.
- Verworfen: `!ScummVMmetadata.zip` (8 GB, PDF/UHS-Extras) als zweites Pack - erst, wenn jemand die Extras im Panel vermisst.
- Grund: eXo fuehrt 109 Titel unter `ScummVM SVN`; mit dem festen Plattformnamen aus `CollectionDef` blieb ihre Galerie leer.
- Gotcha: Alle Poster-Tarballs jetzt mit `COPYFILE_DISABLE=1 --no-mac-metadata`; die drei aelteren tragen noch AppleDouble-Eintraege (harmlos, §16).

## 2026-09-08 - Player: Klick ueberstimmt Pausengruende, verweigerter Start wird benannt, Vorschau beansprucht die Lautsprecher beim Planen
- Entscheidung: `playTheme`/`playFromList` per Klick raeumen die Pausengruende wie `togglePlay` (`listenerWantsSound`); nur der Panel-Autoplay (`playTheme(g, { auto: true })`) laedt still und laesst Video oder Spiel die Lautsprecher. Ein `play()`, das das Element verweigert, steht als `musicPlayError` in der Leiste (Hero-Video analog `videoError`), `AbortError` zaehlt nicht (das ist ein `pause()`/neuer Src auf einen laufenden Start, `playSeq` verwirft veraltete Antworten), `NotAllowedError` nennt ▶ als Ausweg. Der Transportzustand folgt zusaetzlich dem `playing`-Event. Ein zu Ende gelaufener Track (`ended`) wird von einem zurueckgezogenen Grund nicht neu gestartet. Der unmuted Video-Autoplay nimmt `pauseFor("video")` schon beim Planen, nicht erst in `onPlay`.
- Verworfen: Alle blockierenden Panel-Notizen den Play-Knopf deaktivieren lassen (ein falsch-negativer Resolver naehme den Knopf ganz weg; nur `pending`-Notizen lassen ihn spinnen). Transport-Glyphen per Variation Selector auf Textform zwingen (Linux-UI-Fonts haben U+23F8 gar nicht, Fallback bleibt die Emoji-Schrift) - SVG-Icons stattdessen.
- Grund: Linux-Report: Pause-Knopf mit farbigem Emoji ueberlagert, danach ein ▶, das nichts tut. Der Panel-Play auf den geladenen Track war unter einem Pausengrund ein No-op, und ein verweigerter Start hinterliess keine Spur. Der 1-2-s-Ueberlapp Theme/Video aus dem Eintrag vom 06.09. ist damit geschlossen.
- Gotcha: Die Ursache des Linux-Falls ist NICHT gemessen - die Meldung in der Leiste ist das Messinstrument fuer den naechsten Report. Pause und × schalten den Theme-Autoplay weiter fuer die Session ab (Entscheidung vom 06.09.); "Soundtrack startet nicht automatisch" nach einem Pause-Klick ist das, kein Bug.

## 2026-09-08 - Linux gemessen: Dev lief im Software-Renderpfad, der Fetch-Fortschritt kam aus dem falschen Zaehler
- Entscheidung: `pnpm tauri` exportiert kein `WEBKIT_DISABLE_DMABUF_RENDERER=1` mehr (Initial-Commit-Relikt, uebersteuerte §17 in JEDEM Dev-Lauf); der Media-Fetch publiziert waehrend des Streams `file_progress` der Session per 1-Hz-Ticker als Fortschritt (Cap 0.99, nur Phase "fetching"); `btn-spinner-rotate` traegt beide Keyframes voll aus; `.back-to-top` sitzt ueber der Player-Leiste (`--player-h`); Library-Scrollbar 12 px mit 2-px-Inset und mehr Kontrast; der Capitalize/Margin-Hover auf `.download-sheet-label.is-link` ist raus; ein Panel-Autoplay parkt hinter einem laufenden Track (`deferredTheme`, spielt bei `ended`, jeder Klick verwirft ihn). Regressionen in `e2e/specs/render.e2e.ts`, Queue-Verhalten in `music.test.ts`.
- Verworfen: `file_progress` im Frontend pollen (kopiert den 15k-Datei-Vektor pro Frage, §11); den Reader-Fortschritt glaetten (der `read_exact` haengt am 8-MB-Piece, es gibt nichts zu glaetten); `will-change` auf allen Karten gegen das Flackern (nicht gemessen - das Flackern war mutmasslich der Software-Rasterizer, erst nach dem Skript-Fix neu ansehen).
- Grund: Gemessen im Xvfb-Labor (Host-WebKitGTK 2.52.6, A/B am selben Cache-File): MIT dem Flag laeuft `play()` und `currentTime`, aber 0 praesentierte Frames (rVFC in 2 s); OHNE 60-120 Frames - "Video spielt nicht ab" war der Renderpfad, nicht Codec/Server/Autoplay. Und 53 s Phase "fetching" bei 0.000, dann ready: der Chunk-Callback schweigt, solange der Read auf dem Piece wartet.
- Gotcha: Der Spinner stand, weil `rotate(360deg)` als Matrix die IDENTITAET ist - Basis-`translateZ(0)` + nur-`to`-Keyframe heisst Matrix-Interpolation Identitaet->Identitaet. Back-to-top reproduzierte unter echten X11-Wheel-Events in beiden Renderpfaden nicht (Klasse und Paint per Screenshot verifiziert); die Ueberlappung mit der offenen Player-Leiste war der plausibelste Taeter, der Wayland-Eingabepfad bleibt als Rest offen. `stream_file` direkt nach Sessionstart antwortet "invalid state: initializing" und landet als Retry-Knopf im Panel - bekannt, unbehandelt.

## 2026-09-08 - MediaError 4 war transient; ein geschlossenes Panel zieht seinen Auto-Theme-Wunsch zurueck
- Entscheidung: Das Hero-Video bekommt EINEN stillen Retry pro Zeile (800 ms, danach steht der Fehler wieder in der Zeile); `withdrawAutoTheme` nimmt beim Schliessen/Wechseln des Panels einen noch nicht eingetroffenen Autoplay-Wunsch zurueck (`wantedAuto`-Flag, auch fuer den geparkten `deferredTheme`) - der Fetch laeuft fuer den Cache weiter, ein Klick-Wunsch bleibt unantastbar.
- Verworfen: Den Fehler sofort anzeigen ohne Retry (der eine gemessene Fall verschwand beim Replay); den Musik-Fetch beim Panel-Schliessen abbrechen (die Bytes sind das Teuerste am Vorgang).
- Grund: Live-Replay derselben Datei in derselben Instanz auf dem NVIDIA-Pfad: 66 praesentierte Frames in 2,5 s, kein Fehler; alle 39 Cache-MP4s dekodieren per ffmpeg fehlerfrei - der MediaError 4 war ein einmaliger Pipeline-Fehler (mutmasslich unter Resize-Last), keine kaputte Quelle. Und der Hintergrund-Sound ohne Panel war der Theme-Autoplay, dessen Torrent-Fetch das Panel ueberlebte.
- Gotcha: Resize-Zittern auf dem schnellen Pfad ist der Preis von `__NV_DISABLE_EXPLICIT_SYNC=1` - Explicit Sync stuerzt auch auf WebKitGTK 2.52.6 mit Error 71 ab (nachgemessen; der Treiber liest `=0` als "nicht deaktiviert", so reproduziert man es).

## 2026-09-08 - Linux malt Videoframes selbst: Canvas-Spiegel statt WebKitGTK-Praesentation
- Entscheidung: `videoCanvas.ts` spiegelt das Videoelement per `requestVideoFrameCallback` + `drawImage` in ein Canvas (Hero cover, Lightbox contain, dort ohne native Controls - Klick auf den Spiegel togglet); das Element behaelt Opacity 0.02 (nahe null, damit keine Engine-Heuristik ein "unsichtbares" Video suspendiert) und bleibt Tonquelle, Uhr und Klickziel. Nur auf Linux; Ton des Previews faedet ueber 600 ms ein, gegenlaeufig zum Musik-Fade-out.
- Verworfen: Env-Schalter pro Treiber-Stack (`GST_PLUGIN_FEATURE_RANK=nvh264dec:NONE` aenderte NICHTS am Gruen - es ist die Praesentation, nicht der Decoder; `WEBKIT_GST_DMABUF_SINK_DISABLED` existiert, waere aber das naechste Ein-Stack-Pflaster). Thomas: "rocksolid, nicht bei jedem User debuggen".
- Grund: Gemessen in der Live-Instanz auf dem NVIDIA/Wayland-GPU-Pfad: rVFC feuert mit der Datei-Framerate (61/2,4 s bei 25 fps), `drawImage` liefert bewegte, korrekt gefaerbte Pixel (animierte Testdatei, 5 verschiedene Mittelwerte, Loop im currentTime) - waehrend der Schirm gruen/stehend blieb. Decode und Rueckgabe sind heil, nur der Praesentationspfad ist kaputt.
- Gotcha: Der Media-Server sendet jetzt CORS `*` (Token bleibt der Schutz) - noetig fuer getImageData, nicht fuer das reine Zeichnen. Fit-Mathe in `videoCanvas.test.ts`.

## 2026-09-08 - Gruene Videos: WebKits DMABuf-Videosink auf NVIDIA, und warum alle Env-Tests davor nichts bewiesen
- Entscheidung: Der Start setzt auf NVIDIA zusaetzlich `WEBKIT_GST_DMABUF_SINK_DISABLED=1` (Fallback auf WebKits GL-Sink; bereits gesetzte Variable wird respektiert); der Canvas-Spiegel aus dem Vormittag ist per `video_mirror_needed` (Backend: Linux + proprietaeres NVIDIA) auf exakt dieselben Stacks begrenzt statt auf ganz Linux.
- Verworfen: Spiegel auf allen Linux-Systemen (wo der Zero-Copy-Sink funktioniert, ist Readback nicht garantiert - der Spiegel haette gesunde Mesa-Systeme brechen koennen); Decoder-Demotion (gst-launch mit nvh264dec liefert ein perfektes Bild, der Decoder ist unschuldig).
- Grund: Mit aktivem Sink: Schirm gruen, `drawImage` liefert nur die ersten Momente echte Pixel, danach transparent/nichts. Mit deaktiviertem Sink: Schirm, rVFC und Readback korrekt (gemessen nach Seek hinter das Logo-Intro, bewegte korrekt gefaerbte Frames).
- Gotcha: **`pnpm tauri dev` verliert Inline-Env-Variablen auf dem Weg zur App** - `/proc/<pid>/environ` der so gestarteten exodium enthielt sie NICHT, direktes `cargo run` (vite separat) liefert sie bis in den WebProcess. Ein halber Nachmittag "der Schalter aendert nichts" kam daher; kuenftig jedes Env-Experiment per environ-Check verifizieren. Und `pkill -f "tauri dev"` toetet nur den Wrapper: das App-Fenster ueberlebt an systemd reparented, der Nutzer testet dann ein altes Binary.

## 2026-09-08 - Die Decoder-Demotion war selbst der Gruenmacher; nvh264dec + deaktivierter Sink ist der korrekte Pfad
- Entscheidung: `cea1d18c` (nv*dec:NONE auf NVIDIA) revertiert. Endzustand fuer NVIDIA: `WEBKIT_GST_DMABUF_SINK_DISABLED=1` + unangetasteter Decoder + Canvas-Spiegel; sonst nichts.
- Verworfen: Software-Decode als "Parsec-/Spiel-Kontention-Haertung" - die Kontentions-Theorie war unnoetig, alle Gruen-Sichtungen nach dem Sink-Fix liefen mit der Demotion.
- Grund: A/B in derselben Instanz, gleiche Datei, Sink jeweils aus: MIT nv*dec:NONE liefert drawImage konstant RGB(0,77,0) (genulltes Chroma); OHNE liefert es bewegte, korrekt gefaerbte Frames (97/89/37 -> 116/109/73 -> 34/26/16). WebKits avdec-Pfad ist auf diesem Stack der kaputte, nicht NVDEC.
- Gotcha: "Haertung" ohne eigene Messung ist ein Bugbeschleuniger - die Demotion wurde eingebaut, WEIL der fruehere (wirkungslose, s.o. Env-Verlust) Test nichts gezeigt hatte. Jede Renderpfad-Aenderung braucht ihren eigenen Pixel-Beleg.

## 2026-09-08 - Nachzuegler des Videoabends: Geisterton, Mute-Suspension, Theme-Politik
- Entscheidung: Media-Elemente pausieren beim Unmount explizit (Hero + Lightbox) und der Fehler-Retry prueft `isConnected`; der NVIDIA-Start setzt zusaetzlich `WEBKIT_GST_ALLOW_PLAYBACK_OF_INVISIBLE_VIDEOS=1`; Theme-Autoplay folgt "explizit schlaegt implizit" (gewaehlter Track parkt neue Panel-Themes hinter sich, Autoplay ersetzt Autoplay, ▶ adoptiert); jeder Trackwechsel bei laufender Musik bekommt 280 ms Fade-out vor dem Swap, der Fade-in startet erst am `playing`-Event.
- Verworfen: Auf macOS-Verhalten vertrauen (dort pausiert WebKit entfernte Videos selbst - WebKitGTK folgt der Spec und spielt den TON weiter); Fade-in ab dem `play()`-Aufruf (die Decoder-Aufwaermzeit frass die halbe Rampe).
- Grund: Gemessen per Event-Log am echten Hero-Video: die Mute-Suspension pausierte das stumme 0.02-Opacity-Element nach Sekunden (`pause` -> Canvas blendet aus, Cover zurueck, Element schimmert durch); mit dem Schalter laeuft gemutetes Playback durch, `is-visible` bleibt. Der "Async-Leak" war der Spec-Ton entfernter Elemente plus ein Retry auf ein detached Element.
- Gotcha: Die Event-Log-Falle als Werkzeug behalten: Hook auf `video.game-detail-hero-video`, Report ~10 s nach der letzten Nutzeraktion - drei Anlaeufe brauchte die Ausloesebedingung (Lade-Events zaehlen nicht als Aktion).

## 2026-09-08 - Release-Stolperfallen: [skip ci] am HEAD unterdrueckt auch den Tag-Build, Workspace-Datei braucht packages
- Entscheidung: Beim Draft-Neubau nie einen `[skip ci]`-Commit als HEAD taggen - GitHub wertet die HEAD-Message des Push-Events fuer ALLE Workflows, der force-gepushte Tag loeste deshalb nichts aus (workflow_dispatch war der Ausweg). Und `pnpm-workspace.yaml` traegt `packages: ["."]`: ihre blosse Existenz macht das Repo zum Workspace, und CIs ungepinnte pnpm (action-setup@v4 ohne Version, kein packageManager-Feld) holt die neueste Major, die das Feld erzwingt.
- Verworfen: pnpm in CI pinnen (waere die grundsaetzlichere Antwort, aber ein groesserer Eingriff als der Anlass - bei der naechsten pnpm-Ueberraschung `packageManager` in package.json setzen).
- Grund: Der konsolidierte Push endete mit dem Doku-Commit; kein einziger Workflow lief an, und der erste dispatchte Lauf fiel dann ueber die Workspace-Datei (lokal pnpm 11.3: toleriert; CI pnpm 12: "packages field missing or empty").
- Gotcha: Ein Monitor auf "alles gruen" muss nach Commit-SHA filtern, nicht nach "keine laufenden Jobs" - die alten gruenen Laeufe sahen sonst wie das Ergebnis aus.
## 2026-09-09 - Hero-Video: ein Element, ein Besitzer (`stores/heroVideo.ts`)
- Entscheidung: Das Panel haelt genau ein `<video>` fuer seine Lebensdauer und setzt nie selbst `src`; der Controller schaltet Quelle, Start-Timer und Play-Sequenz um und hebt bei jedem Uebergang den vorigen auf (`showPreview`/`clearPreview`/`replayPreview`/`setPreviewMutedNow`/`setLightbox`). Der Pausengrund "video" wird nur dort genommen und zurueckgegeben. Backend-`timed out` bekommt in beiden Medien-Stores einen automatischen zweiten Versuch (10 s), danach "No peers yet - retry".
- Verworfen: `<Show when={videoSrc()}>` behalten und beim Unmount pausieren - schliesst nur das abgeloeste Element, nicht den veralteten `ref` und die Handler des alten `play()`-Promises, die in den Zustand des naechsten Spiels schreiben.
- Grund: Windows-Report: Prince-of-Persia-Trailer lud, spielte nicht, und lief bei einem anderen Spiel unsichtbar und ohne Bedienung los - ein aus dem DOM entferntes Media-Element spielt in Chromium weiter, sobald die Daten da sind. "Retry" bei den ersten Videos/Themes nach der Installation: 45-s-Index-Deadline ohne Peers, neben einem laufenden Pack-Download.
- Gotcha: Die Controller-Uebergaenge laufen unter `untrack` - sie werden aus Effekten gerufen und schreiben die Signale, die sie lesen; ohne das lief der Hero-Effekt im Panel-Test 2.784-mal statt 3-mal. Das Windows-Verhalten selbst ist damit erklaert, nicht gemessen; der naechste Build entscheidet.

## 2026-09-11 - Overlay-Sprachvarianten installieren das englische Spiel als Abhaengigkeit
- Entscheidung: Ein LP-Archiv unter 5 % seines englischen Gegenstuecks ist ein Patch. Die englische Zeile wird dann als eigener Bibliothekseintrag mitgeladen (Event `dependency-download-started`, damit das Frontend sie pollt und entpackt), und die Extraktion legt den englischen Baum per Reflink (`clonefile`/`FICLONE`, sonst Kopie) in das LP-Verzeichnis, bevor der Patch darueber entpackt wird. Gemessen: 91 GLP, 130 SLP, 4 PLP.
- Verworfen: Union-Mount per Symlink im Staging-Verzeichnis (spart die Kopie, aber das Verzeichnis wird pro Start neu gebaut - neu geschriebene Dateien, also Spielstaende, waeren weg; Hardlinks wuerden stattdessen in die englische Installation schreiben). Ebenso ein Katalogfeld fuer die Archivgroesse: die `.torrent`-Dateien liegen ohnehin in der App.
- Grund: Alien Odyssey DE ist 1.168 Bytes gross und enthaelt `GAME.CFG` + `run.bat`; dessen `call game` endete in "Illegal command: game". Die vorhandene Overlay-Erkennung in `fetch_game_variants` konnte nie greifen, weil `download_size` Archiv PLUS gemeinsame GameData ist (DE: 17,9 MB statt 1.168 Bytes).
- Gotcha: `lp_autoexec_compatible` uebersprang `mount`-Zeilen und loeste das folgende `cd` gegen die Staging-Wurzel auf - deshalb lief das Spiel ueberhaupt auf dem Ersatzpfad. Gelesen wird die Zeile jetzt nur, wenn sie das Spielverzeichnis selbst mountet; der Collection-Root-Mount behaelt die alte Bedeutung (Test `patch_dosbox_conf_lp_overlay_root_mount_cd`).

## 2026-09-11 - Notiz-Slot animiert seine Hoehe; abhaengiger Download nimmt seine Wartenden mit
- Entscheidung: Die Panel-Notiz sitzt in einem Slot, der zwischen `grid-template-rows: 0fr` und `1fr` animiert; die Notiz bleibt fuer die Dauer der Schliessanimation gemountet (`shownNote`), geoeffnet wird nach zwei Frames. Ein Abbruch des englischen Basis-Downloads bricht die wartenden Overlay-Varianten mit ab (`cancel_download` liefert sie zurueck, Toast nennt sie), und solange die Basis fehlt meldet der Fortschritt `waiting_for` statt "Extracting".
- Verworfen: Platz fuer die Notiz dauerhaft freihalten (leerer Kasten) und die Notiz anderswo anzeigen (inkonsistent zu den uebrigen Notizen).
- Grund: Die Abhaengigkeits-Notiz verschwindet beim Klick auf Download, und der Rumpf sprang. Und ein abgebrochener Basis-Download liess die Uebersetzung dauerhaft auf "Extracting" stehen, obwohl sie auf Dateien wartete, die nie kommen.
- Gotcha: Der geclippte Kasten darf weder Padding noch Rahmen tragen - ein border-box-Element wird nie kuerzer als beide zusammen, die Notiz behielt so 20px im eingeklappten Zustand (gemessen headless). Deshalb die Zwischenebene `.game-detail-note-clip`.

## 2026-09-11 - Abhaengigkeit wird abgeleitet, das Raster deinstalliert die Gruppe
- Entscheidung: Die Beziehung zwischen Overlay-Variante und englischer Basis wird aus den gebuendelten Torrents abgeleitet (`base_for`, neu `dependents_of`), nicht gespeichert. Die Basis wird NIE automatisch entfernt; `installed_with` zeigt sie im Panel. Das Kontextmenue auf Karte und Listenzeile deinstalliert alle installierten Zeilen der zusammengefuehrten Karte (`performGroupUninstall`), das Panel bleibt bei genau einer Variante (§12).
- Verworfen: eine Spalte `installed_for` auf der Basis (bis zu drei Uebersetzungen teilen sich eine Basis - gemessen an 3SKULLS, KQ5, LSL3); automatisches Mitentfernen der Basis (Thomas' Entscheidung: ein Spiel, das einmal installiert war, verschwindet nicht von selbst).
- Grund: Report "ich kann nur EN loeschen, DE bleibt installiert". Ursache ist aelter als die Abhaengigkeit: `GameCard`/`GameRow` uebergaben die zusammengefuehrte Gruppenzeile, also meist die englische, und nur das Panel kannte die ausgewaehlte Variante.
- Gotcha: `reset_game_data` war fuer Overlay-Varianten kaputt - es entpackte nur das Patch-Archiv und liess ein Verzeichnis mit zwei Dateien als installiert zurueck. Beide Kopierpfade nehmen jetzt zusaetzlich das Lock der Basiszeile.

## 2026-09-11 - Layout-Sprung beim Sprachwechsel: die Galerie bleibt stehen
- Entscheidung: Der Metadaten-Effekt leert `metadata()` nur noch beim Wechsel des SPIELS, nicht der Variante (`lastMetaCard`) - Galerie und Ladezeile bleiben, wo sie sind. Die Hinweiszeile "English description" wird eingeklappt statt ausgehaengt, ueber dieselben Slot-Klassen wie die Panel-Notiz (`.game-detail-collapse`).
- Verworfen: den ganzen Infobereich pauschal animieren (bewegt bei jedem Chip-Klick sichtbar viel); fehlende Felder mit Gedankenstrich rendern (`field()` faellt ohnehin auf die englische Zeile zurueck, die Felder waren nie die Ursache).
- Grund: Headless gemessen, Panelbreite 460px: Aktionsleiste bei EN auf 522px, nach dem alten Wechsel auf 625px - 103px Sprung, verursacht vom Aus- und Wiedereinhaengen der Galerie samt 10px `gap`. Nach der Aenderung 522px in beiden Zustaenden.
- Gotcha: Die Beschreibungslaenge bleibt bewusst ungefixt - sie steht in `.game-detail-scroll` mit eigenem `overflow`, verschiebt also weder Leiste noch Galerie. Der eingeklappte Slot kostet dauerhaft 8px `gap`, aber konstant.

## 2026-09-11 - `get_game` liefert die Sprachliste mit, `fetch_game_by_id` bleibt schlank
- Entscheidung: Der Tauri-Befehl `get_game` haengt `attach_language_maps` an die einzelne Zeile; `fetch_game_by_id` selbst bleibt unveraendert.
- Verworfen: die Gruppenabfrage in `fetch_game_by_id` - das ist der heisse Pfad des 1-Hz-Download-Polls und mehrerer Launch-Schritte, die die Sprachliste nie brauchen.
- Grund: Nach jeder Bibliotheksaenderung liest das Panel seine Zeile ueber `get_game` neu (`Library.tsx`). Ohne `available_languages` ist `isMultiLang()` falsch, und die Sprach-Chips samt Download-Optionen der anderen Fassungen verschwanden - gemeldet als "nach Uninstall keine Sprachdownload-Optionen mehr".
- Gotcha: Der Fehler ist aelter als die Overlay-Abhaengigkeit und trat bei jeder Deinstallation auf; er faellt erst auf, seit eine zweite Zeile derselben Gruppe interessant ist. Test `a_single_row_still_gets_its_groups_language_map`.

## 2026-09-11 - Der Bestaetigungstext muss sagen, was der Aufrufer wirklich tut
- Entscheidung: `GameActionsMenu` bekommt die Eigenschaft `uninstallsGroup`; nur Karte und Listenzeile setzen sie, und nur dann zaehlt der Bestaetigungstext die Fassungen. Das Panel bleibt bei der ausgewaehlten Variante (§12).
- Verworfen: den Zaehler an das blosse Vorhandensein von `onUninstall` haengen - beide Aufrufer setzen die Eigenschaft, also versprach das Panel "2 versions" und entfernte eine.
- Grund: Gemeldet als "sagt 2 Versions, loescht aber nur EN". Der Text kam vom Gruppen-Zaehler, die Handlung vom variantengenauen Panel-Handler.
- Gotcha: `performGroupUninstall` selbst war korrekt - Test `groupUninstall.test.ts` haelt das fest, damit die naechste Aenderung nicht an der falschen Stelle sucht.

## 2026-09-11 - Code-Review: Groessenverhaeltnis allein ist kein Klassifikator
- Entscheidung: Ein Patch muss BEIDES sein - unter 5 % des englischen Archivs UND hoechstens 64 KB gross. Dazu: die geteilte `!save`-Sicherung wird fuer eine lokalisierte Variante nicht mehr zurueckgespielt; ein Abbruch wirkt jetzt in beide Richtungen (Basis ohne verbleibende Interessenten geht mit); `brokenImages` wird bei jedem Variantenwechsel geleert; der Metadatenzustand ist waehrend des Ladens busy, damit der Handbuch-Knopf nicht die vorige Fassung oeffnet; die Lightbox haelt die Lautsprecher nach dem GEZEIGTEN Eintrag, nicht nach dem, bei dem sie geoeffnet wurde; ein Spielstart stoppt die Vorschau.
- Verworfen: nur das Verhaeltnis zu verschaerfen. Die Verteilung ist stetig von 0 bis 1, es gibt keine Luecke - 57 Zeilen unter 5 % sind vollstaendige Diskettenfassungen, deren englisches Gegenstueck die CD-Fassung ist (King's Quest VI DE 24,9 MB gegen 710 MB).
- Grund: Review-Befund. Der Preis eines Fehlurteils ist ein ungefragter 710-MB-Download plus ein gemischtsprachiger Installationsstand; der Preis eines verpassten Patches ist ein Spiel, das nicht startet und erneut geladen werden kann. Also konservativ.
- Gotcha: Gemeldeter Fehler "spanisches Alien Odyssey startet auf Deutsch" war die geteilte Sicherung: Deinstallieren verschiebt das GANZE Spielverzeichnis nach `!save/<sc>` (§5), und die Wiederherstellung beim Entpacken griff eine Ebene hoeher - 565 MB englisches Spiel landeten ueber der frisch entpackten spanischen Fassung (nachgewiesen an `run.bat`, 721 statt 756 Bytes). Test `a_localized_variant_never_restores_the_shared_save_backup`.

## 2026-09-11 - eXos eigene Liste entscheidet, welche Sprachvariante die englische Basis braucht
- Entscheidung: `metadata/multilanguage_<sprache>.txt` (eXos `util/<lang>/multilanguage.txt` aus dem Metadata-Archiv des Packs) wird mitgeliefert und von `lp_overlay::base_for` gelesen, geschluesselt auf den Namen der Starter-Bat aus `application_path` - genau das `%GameName%`, das eXos Installer nachschlaegt. Die Groessenregel bleibt nur noch als Rueckfall fuer ein Pack ohne Liste.
- Verworfen: Groesse als Klassifikator. Gegen eXos Antwort gemessen fand sie bei Spanisch 67 von 228 und bei Deutsch 70 von 92; ohne absolute Obergrenze erfand sie zusaetzlich 57 Falschtreffer (King's Quest VI DE, 24,9 MB Diskettenfassung gegen 710 MB englische CD, liest sich als 3 %).
- Grund: eXos Installer macht genau dasselbe wie wir - `unzip eXoDOS\<Spiel>.zip -d eXoDOS\!german\` und dann das Sprachpaket darueber - aber gegated auf diese Liste. Symlinks scheiden aus, weil das Spiel in seinen eigenen Ordner schreibt (Alien Odysseys `run.bat` loescht und kopiert Treiberdateien); ueber einen Link landete das in der englischen Installation.
- Gotcha: Die Liste steckt nicht in `util.zip`, sondern im grossen `<Pack>_Metadata.zip`, und nennt englische Bat-Namen, nicht die lokalisierten Titel. Geholt mit `examples/media_pack_spike` und `EXTRACT=...`; der Befehl steht im Kopf der Dateien. Polnisch fehlt noch (Torrent gibt gerade nichts her), dort greift der Rueckfall.
- Messung: Eine geklonte Kopie kostet auf APFS null Bytes (freier Platz vorher/nachher identisch, `du` meldet trotzdem 565 MB). Nur NTFS und aeltere Linux-Dateisysteme zahlen den vollen Preis.

## 2026-09-11 - Die angezeigte Groesse ist die, die der Klick wirklich kostet
- Entscheidung: `get_game_variants` addiert das englische Archiv nur, wenn dessen Spielverzeichnis fehlt; `requires_base` wird genau dann gesetzt. Geprueft wird das Verzeichnis, nicht das `installed`-Flag - es ist die Bedingung, an der auch die Extraktion haengt.
- Verworfen: immer beides zu addieren (war der Zustand: 476 MB fuer einen 1.168-Byte-Patch neben einer bereits installierten englischen Fassung) und das `installed`-Flag zu befragen (kann ohne Verzeichnis gesetzt sein).
- Grund: Report "DE zeigt volle Groesse, obwohl EN installiert ist". Gemessen nach der Aenderung: EN 476,2 MB, DE 17,9 MB (Patch plus gemeinsame GameData), ES 474,1 MB.
- Gotcha: Die 17,9 MB sind fast vollstaendig die gemeinsamen Extras aus dem englischen Archiv (§6), der Patch selbst ist gut ein Kilobyte.

## 2026-09-11 - Waehrend Zuruecksetzen und Deinstallieren steht die Leiste still
- Entscheidung: `busyOp()` fasst beide zerstoerenden Vorgaenge zusammen; die ganze Aktionsleiste weicht einer Zeile mit Spinner, und die Sprach-Chips sind gesperrt. Vorher tat das nur die Deinstallation, ein Zuruecksetzen ersetzte lediglich den ⋯-Knopf.
- Verworfen: nur die einzelnen Knoepfe deaktivieren - die Leiste haette ihre Hoehe behalten, aber Spielen, Handbuch und Herunterladen waeren weiter sichtbar gewesen und das Umschalten der Fassung haette den Zustand versteckt.
- Grund: Report waehrend eines echten Zuruecksetzens: es dauert Minuten (1,1 GB Sicherung loeschen, 474 MB auspacken) und in der Zeit war alles andere anklickbar.
- Gotcha: Das Backend serialisiert ohnehin ueber `game_op_lock`, ein zweiter Klick waere also nicht gefaehrlich, aber er sah aus wie ein haengendes Programm.

## 2026-09-11 - Zweite Review-Runde: wer den Download angestossen hat, muss bekannt sein
- Entscheidung: `auto_queued_bases` merkt sich pro Sitzung, welche englische Zeile Exodium selbst fuer eine Uebersetzung eingereiht hat; nur die darf beim Abbrechen mitgehen. Nach einem Neustart ist die Menge leer, es wird also nichts automatisch abgebrochen. Dazu: die Platz-Vorabpruefung zahlt die Basis nur, wenn ihr Verzeichnis fehlt, und rechnet mit der ARCHIVGROESSE statt mit `download_size` (das enthaelt die gemeinsamen GameData ein zweites Mal). Deinstallieren einer Basis bricht wartende Uebersetzungen ab und sagt es in der Rueckmeldung. Zuruecksetzen prueft Basis und freien Platz unter dem Lock, bevor es loescht. Eine Zeile ohne Starter-Pfad faellt auf die Groessenregel zurueck statt still als eigenstaendig zu gelten.
- Verworfen: die Basis am `in_library`-Flag zu erkennen - ein selbst gestarteter englischer Download sieht genauso aus und wurde mit abgebrochen, samt Meldung "wurde mit abgebrochen, weil die Uebersetzung sie braucht", was das Gegenteil des Geschehenen war.
- Grund: Review-Befunde. Die Vorabpruefung verlangte fuer spanisches Phantasmagoria 2 rund 20 GB frei, wo 3,2 GB geholt werden. Und wer die englische Fassung deinstallierte, waehrend eine Uebersetzung auf sie wartete, bekam einen Wartezustand ueber Neustarts hinweg, aus dem nur Abbrechen herausfuehrte.
- Gotcha: `dependents_of(.., false)` liefert die wartenden (in_library, nicht installiert), `true` die installierten - beide Aufrufe stehen jetzt in `uninstall_game` bzw. `get_game_variants`.

## 2026-09-11 - Kopiert wird erst, wenn die englische Extraktion FERTIG ist
- Entscheidung: `base_ready` = `installed` UND Verzeichnis vorhanden; Installation und Zuruecksetzen warten darauf. Das Verzeichnis allein gilt nicht mehr.
- Verworfen: auf `base_game_dir` allein zu gatern - genau das war der Fehler.
- Grund: Im Feld gemessen bei deutschem Alien Odyssey: das CD-Abbild lag mit 43.861.320 statt 558.480.048 Bytes in der deutschen Kopie, weil das englische Archiv noch entpackt wurde. Das Spiel startete in ein schwarzes Fenster und beendete sich nach zehn Sekunden mit Status 0.
- Gotcha: Ein Verzeichnis entsteht mit der ERSTEN Datei, die der Entpacker schreibt. Bestehende halbe Kopien heilen nicht von selbst - "Zuruecksetzen" im Panel ist der Weg.

## 2026-09-11 - Der cd-Zielpfad wird ohne Ruecksicht auf Gross- und Kleinschreibung aufgeloest
- Entscheidung: `resolve_ignoring_case` sucht bei einem Fehlschlag den Eintrag des Elternverzeichnisses, der sich nur in der Schreibweise unterscheidet.
- Grund: CI auf ubuntu-22.04 fiel ueber `lp_probe_follows_the_mount_target_into_a_subdirectory`; macOS und Windows bestanden ihn, weil ihre Dateisysteme die Schreibweise ignorieren. eXo schreibt `cd odyssey`, der Ordner heisst `ODYSSEY` - unter Linux verwarf die Pruefung deshalb funktionierende Konfigurationen und fiel auf den Ersatz-Autoexec zurueck.
- Gotcha: Der Test dazu haelt auf allen Plattformen, weil er das Ergebnis auf `is_dir()` prueft statt auf die Schreibweise.

## 2026-09-17 - DOSBox Staging 0.83.0: der Shader-Ordner heisst anders und ist jetzt zwei
- Entscheidung: `get-dosbox.sh` holt 0.83.0 und legt `shaders` UND `shader-presets` getrennt ab (`resources/dosbox-shaders`, `resources/dosbox-shader-presets`); `ensure_dosbox_shaders` installiert beide in DOSBox' Konfigverzeichnis, jeweils an einer Sentinel-DATEI erkannt.
- Verworfen: nur die Versionsnummer anzuheben - 0.83.0 hat `glshaders` in `shaders` umbenannt, die Staging-Suche lief ins Leere, und der Lauf endete mit "ABORT: RENDER: Error setting fallback shaders".
- Grund: Gemessen mit dem frisch geholten Binary. Ohne `shader-presets` startet es zwar, meldet aber "Cannot locate shader preset 'crt/crt-hyllian:hercules'" und faellt auf einen einfachen Shader zurueck - der CRT-Modus waere still verschwunden.
- Gotcha: Der Schluessel `glshader` gilt weiter als veraltetes Synonym fuer `shader`, ebenso `windowresolution` fuer `window_size` - die Startfragmente und alle 750 eXo-Konfigurationen (alle mit `sharp`) brauchten keine Aenderung. Softwarescaler (`scaler=normal2x`) meldet 0.83.0 dagegen als abgeloest.

## 2026-09-18 - Media-Pack-Spike: Magazine, Buecher und Kataloge sind LaunchBox-Plattformen aus PDFs
- Entscheidung: Der Lesesaal liest die drei Zips des Media Packs (Infohash `cb73d61e6b4ea5eaa793c64c1d80e6113e95de84`, 236,8 GB, 9 Dateien) per `zip_range` ausgabenweise; der Katalog kommt aus den drei `Data/Platforms/*.xml` darin, die eXo als normale LaunchBox-Games fuehrt (Title, ReleaseDate, Publisher, Genre = Publikation, Notes).
- Verworfen: `read_entry_with` fuer Buecher - 13 der 168 liegen ueber dem 512-MiB-Deckel (max 1,49 GB komprimiert) und das Ergebnis ist ein `Vec<u8>`; es braucht einen Reader, der in die Cache-Datei streamt.
- Grund: Gemessen ueber den Live-Torrent: DOSMagazines 102,0 GB / 48.897 Eintraege / Verzeichnis in 7,2 s, DOSBooks 45,0 GB, DOSCatalogs 1,7 GB. Lesbar sind 937 + 168 + 195 = 1.300 PDFs (Magazine Median 57 MB, Buecher Median 222 MB, Kataloge Median 4,3 MB) in 19 Serien; dazu 2.490 + 311 + 336 Bilder unter `Images/<Plattform>/Box - Front` als Tier-0-Quelle.
- Gotcha: 241 Magazin-Ausgaben sind keine Scans, sondern startbare Disk-Magazine (`.bat` + CommandLine, Big Blue Disk, PC-Gamer-CDs mit 600-MB-ISOs). Und `DOS_linux_Magazines.zip` (2,8 MB) enthaelt 910 Verknuepfungen `!dos/<shortcode>/Magazines/<Art> <Magazin> <Datum> page <N>` - 486 von 487 Shortcodes matchen exakt auf `games.shortcode` (nur eXos Tippfehler `caeser2`), das ist der Spiel->Seite-Index fertig geliefert.

## 2026-09-18 - Lesesaal: eigene Tabellen, ganz ersetzt, Nutzerzustand daneben
- Entscheidung: `publications`, `issues` und `game_articles` sind reiner Katalog und werden beim Refresh komplett getauscht (`copy_catalog_tables`, Kinder zuerst geloescht, Eltern zuerst eingefuegt); der Nutzerzustand liegt in `issue_state`, keyed auf `issues.key` statt auf eine Row-Id. Der Media Pack ist ausserdem KEIN `CollectionDef`, sondern `MEDIA_SOURCES` mit faul erzeugtem Manager.
- Verworfen: Zeilen in `games` mit einer `kind`-Spalte (jede WHERE-Klausel, jeder COLLECTION_MAP-Projektor und `download_game` haetten die neue Art ausschliessen muessen); Spaltenschnitt-Abgleich wie bei `games` (unnoetig, sobald kein Nutzerzustand in der Tabelle steht); Offsets aus dem Zip in den Index backen (eXo kann neu packen, und die Reader-Sitzung hat das Central Directory ohnehin).
- Grund: `enable_new_collections` haengt jede neue COLLECTION_MAP-Id an die `collections`-Config - ein Media-Pack-Eintrag haette jedem Bestandsnutzer beim naechsten Start einen 237-GB-Torrent beigelegt, fuer eine "Sammlung", in der nichts installierbar ist.
- Gotcha: Alle Torrents schreiben in EINE Wurzel und `cleanup_placeholder_files` loescht, was nicht auf der Keep-Liste steht - `ensure_manager` muss die Vereinigung neu bilden, sobald ein Torrent dazukommt, sonst raeumt der naechste Lauf die Dateien unter einem laufenden Stream weg.

## 2026-09-18 - Disk-Magazine starten: Subtree in einem Vorwaertslauf, run.bat aus einer Vorlage
- Entscheidung: `install_issue` holt den Ausgaben- bzw. Serienordner (plus den Serien-Bat an der Magazines-Wurzel) mit `read_subtree_to_dir` in EINEM nach Local-Header-Offset sortierten Lauf in die Spielwurzel; `launch_issue` schreibt `run.bat` aus der unberuehrten Vorlage `run.exotpl` neu und startet `<launch_dir>/dosbox.conf` ueber `patch_dosbox_conf` mit Arbeitsverzeichnis `<root>/eXo`.
- Verworfen: eintragsweises Entpacken (ein 8-MiB-Stueck pro Datei, PCGamer_1997_01 hat 934 Dateien auf 410,8 MB) und eXos eigenes Verfahren, run.bat in place zu ersetzen und danach aus `run.bak` zurueckzukopieren.
- Grund: Die Eintraege einer Ausgabe liegen zusammenhaengend im Archiv, der Stream kennt nur Vorwaertsspruenge guenstig. Und ein Absturz zwischen Substitution und Restore laesst run.bat auf eine Ausgabe festgenagelt zurueck - die Vorlage macht den Schritt wiederholbar statt reparaturbeduerftig.
- Gotcha: Die Schwellen der Platzhalter (BBD `YYY` ab >107, `ZZZ` 3000/9000 um 068/069, IECD `YYY` ab >=14) stehen nirgends im Katalog - `gen_media_assets.py` liest sie aus den `sed`-Zeilen der Linux-Twins; die fuenf BBD-Sonderausgaben mit nicht-numerischem CommandLine (`BBDB1`, `ODMBonus`, …) ueberspringen die numerischen Bedingungen genau wie eXos `goto skip`.

## 2026-09-19 - Lesesaal: Heruntergeladenes bleibt, bis der Nutzer es entfernt
- Entscheidung: Kein Cache-Deckel und kein LRU-Lauf mehr; `remove_issue` loescht die Cache-Datei bzw. das entpackte Ausgabenverzeichnis (immer `issue_dir`, nie den geteilten `launch_dir` einer Serie) und raeumt `issue_state.installed` ab, `reading_storage_usage` zeigt Anzahl und Bytes in den Einstellungen.
- Verworfen: Pinnen ("Keep"). Ohne Kehrmaschine gibt es nichts, wovor ein Pin schuetzen wuerde - Spalte, Kommando, Badge und Knopf sind raus.
- Grund: Spiele werden nie automatisch geloescht; ein Buch mit 222 MB Median verhaelt sich fuer den Nutzer genauso, und ein stiller Sweep nimmt ihm etwas weg, das er selbst geholt hat.
- Gotcha: Die `.part`-Regel ueberlebt als eigener Startlauf (`sweep_partial_downloads`) - ein abgebrochener Fetch waere sonst fuer immer ein abgeschnittenes PDF. Und `remove_issue` misst die Behaeltnis-Pruefung an einem EXISTIERENDEN Pfad (`canonicalize` beidseitig); fuer einen schon geloeschten Pfad gibt es nichts zu pruefen und nichts zu tun.

## 2026-09-19 - GLP-Magazin-Addon: ein STORED-Zip im Installer-Zip, Katalog als 654 XML-Fragmente
- Entscheidung: `Content/eXoDOS_GLP_Addonpack_MagazinesGLP.zip` (60,4 GB im GLP-Torrent) wird wie das Media Pack ueber `zip_range` gelesen - das aeussere Zip ist eXos Installer (Bat, CHOICE.EXE, Readmes), das eigentliche Archiv `Content/eXoDOS_GLP_Addonpack_MagazinesGLP_1.0.zip` liegt darin STORED, also per `OffsetReader`. `media_pack_spike` schreibt das innere Verzeichnis jetzt als `.nested.jsonl` und zieht mit `NESTED_EXTRACT` einen benannten inneren Eintrag.
- Verworfen: das Addon als Content Pack auspacken (60 GB, genau der Bulk-Download aus #14); die deutschen Hefte in den Media-Pack-Index mischen, ohne die Quelle zu tragen (`issues.zip_file` meint heute stillschweigend den Media Pack).
- Grund: Gemessen: inneres Verzeichnis 19.136 Eintraege in 2,7-7,9 s; 654 PDFs (56,5 GB, Median 86 MB, max 203 MB) in 9 Serien (Power Play 145, ASM 126, PC-Player 117, DOS International 89, PC Joker 70, PC Games 51, PC Spiel 27, PC Action 23, PC Xtreme 6); 654 Box-Front-PNGs; 270 Cover-CD-Videos (3,6 GB) unter `!mags/!german/<Heft>/Extras/`; 654 LaunchBox-Fragmente je Heft unter `xml/!german/magazines/`; 9.697 Spiel->Seite-Links (`!dos/!german/<code>/Magazines/<Art> <Heft> Seite <N>.bat`, SumatraPDF-Aufruf wie im Media Pack) ueber 2.515 Shortcodes, 2.355 davon im Katalog.
- Gotcha: Die Links zeigen auf `Magazines\!german\<Serie>\<Heft>.pdf` - der Pfad im Addon ist eXos Zielpfad nach dem Merge, nicht der im Zip (`eXo/Magazines/!german/...`); und 160 Shortcodes (`3DChesDE`, `AlienPDE`, ...) sind GLP-eigene Codes, die der Katalog nicht kennt.

## 2026-09-19 - Lesesaal: zweite Quelle ueber `source`/`inner_zip`, fehlender Manager ist ein Fehler
- Entscheidung: `issues.source` nennt den Torrent (`eXoMedia` | `eXoDOS_GLP`), `issues.inner_zip` den STORED-Eintrag im Installer-Zip; jeder Lese-Stream laeuft durch einen `OffsetReader` (Vollfenster = Identitaet), das gecachte Verzeichnis traegt `base`/`len`. Sprache ist eine Spalte (`EN`/`DE`), Publikationen tragen eXos `(DE)`-Suffix. Fehlt der Collection-Manager bei laufender Session, antwortet das Kommando mit `Err` - der bestehende Fehlerpfad zeigt es und fragt beim naechsten Oeffnen neu.
- Verworfen: ein `needs:<collection>`-Status mit eigener UI (es gibt keine Collection-Opt-in-UI, der Zustand ist nur per Hand-Config erreichbar, und `put()` haette den provisorischen Status sofort vergessen); `game_articles.language` (kommt aus dem Join ueber `issues`); ein `de:`-Infix im Key (`!german/` namespaced den Pfad schon).
- Grund: 654 Hefte und 9.697 Spiel->Seite-Links, alle neun Serien lueckenlos - fuer deutsche Nutzer der Hauptweg ins Heft. Gates im Generator: 654/654 PDFs, 9.697/9.697 Ziele, keine Cover-Key-Kollision, eine Sprache je Publikation.
- Gotcha: Die Bats nennen eXos Zielpfad (`Magazines\!german\...`), das Archiv fuehrt `eXo/Magazines/!german/...`; Sonderhefte sind im Dateinamen nullgepolstert, im Titel nicht - der Dateistamm ist der kanonische Titel. `copy_catalog_tables` kopiert die Spaltenschnittmenge, neue Spalten erreichen eine Bestands-DB nur ueber die ALTERs in `migrate()`.

## 2026-09-20 - pdfjs-dist auf 5.6.205 gepinnt: WebKit kompiliert das OpenJPEG-WASM ab 5.7 nicht
- Entscheidung: `pdfjs-dist` exakt `5.6.205`; `scripts/copy-pdfjs-wasm.mjs` scannt die gestageten Module auf Relaxed-SIMD-Opcodes und bricht den Build ab, wenn ein Upgrade sie zurueckbringt.
- Verworfen: nur `openjpeg.wasm` aus 5.6 unter 6.3 legen (die Emscripten-Glue im Worker muss zum Modul passen); auf den JS-Fallback warten (Minuten pro Seite); eigener OpenJPEG-Build ohne Relaxed SIMD (Pflegeaufwand fuer ein Feature, das WebKit irgendwann bringt).
- Grund: Gemessen in einer nackten WKWebView (macOS 26.6, WebKit 21624): `WebAssembly.compile(openjpeg.wasm)` scheitert ab 5.7.284 mit `relaxed simd instructions not supported`, 5.4.624-5.6.205 kompilieren; PC World 146 Seite 9 rendert mit 5.6 in 1,1 s, mit 6.3 nie (Worker rechnet im JS-Fallback, Hauptthread wartet). jbig2.wasm kompiliert in jeder Version - deshalb verschwanden die weissen Seiten mit dem WASM-Staging, die JPX-Seiten blieben leer.
- Gotcha: pdf.js meldet den Fallback nur als `warn` im Worker - im Reader sieht man leere Seitenboxen ohne Fehler. `WebAssembly.validate` unter Node ist kein Test (V8 kann Relaxed SIMD), darum der Opcode-Scan.

## 2026-09-20 - librqbit 9.0.0-rc.0 per `[patch.crates-io]`: Lock-Reihenfolge in `update_only_files`
- Entscheidung: `TorrentStateLive::update_only_files` gibt den State-Write-Lock frei, BEVOR `reconnect_all_not_needed_peers` die Peer-DashMap durchlaeuft (ein Commit auf dem Fork; Basis ist der Commit, aus dem die rc.0 auf crates.io gebaut wurde).
- Verworfen: Upgrade auf 9.0.1 (gleicher Code, `main` ebenfalls); Torrent vor `update_only_files` pausieren (killt alle Peers bei jedem Spielstart-Download).
- Grund: Gemessen per `sample` an der haengenden App: `download_game -> update_only_files` haelt den State-Lock und wartet auf einen DashMap-Shard, `on_peer_died` haelt den Shard (`get_mut`) und wartet auf den State-Lock (`lock_write`); alle Tokio-Worker parken in parking_lot, jedes async-Kommando haengt - im Lesesaal als "Preparing..." ohne Ende sichtbar.
- Gotcha: Der Deadlock braucht einen sterbenden Peer waehrend eines Spielstart-Downloads - selten, aber dann steht die ganze App und nur ein Neustart hilft. Upstream-Issue/PR steht aus.

## 2026-09-20 - Lesesaal-Cover als eigenes Poster-Pack (content-v8)
- Entscheidung: `posters-eXoMedia-v1` ist ein eigenes Content Pack (`content/posters/eXoMedia`, 400 px Q90 wie die Spiel-Packs); die Einstellungen zeigen es in einer eigenen Gruppe "Reading Room", der Banner ueber dem Lesesaal-Grid haengt am selben Trigger wie in Browse.
- Verworfen: die Cover in das eXoDOS-Poster-Pack mischen.
- Grund: Packs werden als Ganzes versioniert - ein Merge haette bei jedem Bestandsnutzer die vorhandenen 397 MB geloescht und alles neu gezogen, fuer 178 MB neuer Kunst.
- Gotcha: Das Media Pack ist keine Collection. Manifest, Install und Adoption fragen nie `COLLECTION_MAP`, also tragen sie die Quelle ohne Zutun; die beiden Frontend-Sweeps zaehlen aber Collections auf und haengen `MEDIA_SOURCE` selbst an - die `collections`-Config darf die Id nie bekommen, sonst liegt ein 237-GB-Torrent beim naechsten Start in jeder Installation.

## 2026-09-20 - Jump-Bar als Layout-Spalte statt Overlay
- Entscheidung: Die Jump-Bar ist eine Flex-Spalte neben dem Scroller (`.library-shell` = `.library` + `.jump-bar-slot`); alle drei Tabs portalen ihre Bar in den einen Slot. Labels kuerzt CSS per Ellipsis an der echten Breite (max 200 px), nicht mehr JS bei 13 Zeichen; Namen-Bars (`.wide`) sind linksbuendig, Buchstaben/Jahre zentriert.
- Verworfen: Overlay behalten und den Grid-Gutter per ResizeObserver auf die gemessene Bar-Breite setzen (JS misst nach, was Layout kann, und die Bar ragte weiter ueber die Toolbar); Publikationen auf Anfangsbuchstaben reduzieren (dann ist die Bar A-Z in anderer Sortierung, die Namen sind der Nutzen).
- Grund: 44 Publikationen ergaben eine ~125 px breite Bar in einem 80-px-Gutter - achte Kartenspalte und Listen-Toggle verdeckt; 21 Namen ueber 14 Zeichen wurden zu Zwillingen ("Computer Gami...", "Computer Grap...").
- Gotcha: Solids `Portal` wickelt seine Kinder in ein `div`; `.jump-bar-slot > div { display: contents }` macht die Bar wieder zum Flex-Item, sonst greift `min-height: 0` nicht und die Bar waechst statt zu scrollen. Ein leerer Slot hat keine Breite - die Raender traegt die Bar.

## 2026-09-20 - Uninstall behaelt nur Nutzerdaten, Archiv-Drop als Option, ehrliche Fehler
- Entscheidung: `back_up_user_data` vergleicht den Spielordner mit dem Central Directory des Archivs (Name, Groesse, dann CRC-32) und sichert nur Neues/Geaendertes nach `!save`; ohne Archiv ersetzt ein Manifest (`content/pristine/<source>_<idx>.idx`, beim Archiv-Drop geschrieben) den Vergleich, ohne beides bleibt der ganze Ordner. `keep_archives` (Default an) entscheidet, ob das ZIP nach dem Entpacken bleibt; Reset ohne Archiv = Wipe + Download. Loeschfehler sind Fehler: Zeile bleibt installiert, Toast traegt den OS-Fehler.
- Verworfen: Ganz-Ordner-Rename beibehalten und nur den Toast ergaenzen (bei ScummVM/Win3x bleibt dann ein Gigabyte-Spiel als "Save" liegen); Fragment-Dateien der Piece-Nachbarn loeschen (Bitfield meldet dann komplette Pieces ohne Bytes - Seeding liefert Muell); Vergleich per mtime statt CRC (das zip-Crate setzt keine mtimes).
- Grund: Discord-Report (Windows): Uninstall "tut nichts", Spiel belegt das Doppelte. Ursache bei uns: `rename` ohne angelegten `!save`-Elternordner scheiterte immer, der Copy-Fallback kopierte Gigabytes, `remove_dir_all` schlug unter Windows fehl und der Fehler wurde verschluckt - DB sagte deinstalliert, Original plus Kopie lagen da. Auf macOS unsichtbar, weil der Fallback dort still durchlaeuft.
- Gotcha: `invalidate_after_file_delete` loescht den Torrent aus der Session und fuegt ihn mit der Restauswahl wieder hinzu - beim Sammel-Entfernen darum EIN Aufruf pro Torrent. librqbit markiert Dateien auch unter Windows sparse (`FSCTL_SET_SPARSE`); Explorers "Groesse" ist logisch, "Groesse auf Datentraeger" echt - darum `allocated_size` fuer die Archiv-Anzeige.

## 2026-09-20 - Settings mit Seitenleiste, Storage-Tab nach Steam-Vorbild
- Entscheidung: `SettingsDialog.tsx` loest die ~300 JSX-Zeilen aus `App.tsx`; fuenf Sektionen (General, Storage, Network, Content Packs, About) statt zwei Tabs, jede Zeile ist `SettingRow` (Label 160 px | Wert + Ein-Satz-Hint | Aktion), Schalter sitzen in der Aktionsspalte. Storage: ein Walk ueber den Datenordner (`storage_overview`, allocated size), Balken + Kategorien mit Aktionen (Archive entfernen, Save-Backups loeschen, Medien-Caches leeren) und die Liste aller installierten Spiele nach Groesse mit Uninstall.
- Verworfen: Kategorien per `du` ueber Ordnernamen (auf macOS/Windows sind `content/` und `Content/` ein Verzeichnis, GameData laege dann bei den Packs); Pack-Archive (die Metadata-ZIPs aus den Torrents, 50 GB) mit Loeschknopf (braeuchte Ledger-Invalidierung im Collection-Torrent, erst anzeigen); Messung pro Oeffnen (dauert auf einer grossen Bibliothek 10-20 s, darum modulweit gecacht, Refresh und jede Aktion messen neu).
- Grund: UX-Review 2026-09-20: drei linke Kanten (Titel, Label, Toggle-Text), keine Label-Spalte, dreizeilige Hints, sieben Sektionen in einem Tab, Speicher-Themen ueber Library, Packs und Danger Zone verstreut.
- Gotcha: `installed`-Zaehlung (116) und Spielordner (93) klaffen absichtlich auseinander - Pass 2 des Scans markiert ein vollstaendiges ZIP ohne Ordner als installiert, das zaehlt bei den Archiven. Zips im Spielbaum werden beim Walk geoeffnet, sonst zaehlen Piece-Fragmente der Nachbarn als Archive (280 statt 52 bei Thomas).
- Gotcha (Review 2026-09-20): `invalidate_after_file_delete` ist jetzt serialisiert (`invalidate`-Mutex) und wartet vor `session.delete` auf `wait_until_initialized` - ein Delete waehrend des Checkens hinterliess den Re-Add mit geschlossenen Dateien (`FsFileIsNone`, Torrent im Zustand `None`, gemessen bei zwei Uninstalls hintereinander). Ohne Handle wird die `.bitv` trotzdem gepatcht. Archiv-Drops nach Install sammeln sich 3 s in `schedule_invalidation`, ein Re-Add pro Torrent statt einem pro Spiel. `get_download_progress` synthetisiert fuer ein installiertes Spiel ohne Handle einen fertigen Fortschritt, sonst laeuft der Tracker in fuenf Null-Polls und meldet "Download didn't start". `fetch_installed_games` ist die Shelf-Query (eine Zeile pro Gruppe) - Storage, Archive und Spieleliste nehmen `fetch_installed_rows`.

## 2026-09-20 - Galerie-Flackern: die Player-Leiste oeffnete sich fuer eine Sonde
- Entscheidung: Die Leiste zeigt, was laeuft, oder was der Hoerer angefordert hat - ein Autoplay-Wunsch des Panels oeffnet sie nicht mehr (`wantedIsAuto`, gelesen in `NowPlayingBar.barTrack`). Die Wartemeldung dazu steht ohnehin im Panel (Theme-Zeile, §14).
- Verworfen: 56 px dauerhaft freihalten (kostet Fensterhoehe fuer eine Leiste, die meist nie kommt); die Galerie aus dem elastischen Scrollbereich nehmen (das Symptom sass nicht dort, die Ursache war die Panelhoehe selbst).
- Grund: Gemessen unter Linux/WebKitGTK (Xvfb-Labor, isoliertes XDG_DATA_HOME, rAF-Sampling des Panel-Layouts): Panel oeffnen -> `playTheme(auto)` setzt `wanted` -> Leiste da -> `body.has-player` -> Panel 800 -> 744 px -> Galerie 703 -> 663 px; die Sonde antwortet "kein Theme", `wanted` faellt weg, alles springt zurueck. 170 ms, zwei Spruenge, bei jedem geoeffneten Spiel. Etwa die Haelfte des Katalogs hat kein Theme, online dauert die Antwort Sekunden statt Millisekunden - dann steht die Leiste sichtbar und verschwindet wieder.
- Gotcha: KEIN 0.15-Regress - dieselbe Messung auf `v0.14.3` (gleiches Backend, gleiche Daten, nur das Frontend getauscht) zeigt exakt dieselben 703/663/703. Die neue "On disk"-Zeile ist unschuldig: sie waechst INNERHALB von `.game-detail-scroll`, und der Bereich ist `flex: 1` mit `min-height: 120px` - er federt, ohne die Leiste darunter zu verschieben (gemessen: `.game-detail-info` 120 px konstant, `media` unbewegt). Die Galerie haengt dagegen an der Panelhoehe, weil sie unter dem Scrollbereich fest sitzt; alles, was das Panel hoeher oder niedriger macht, verschiebt sie. Auf dem Referenzrechner lief dabei der beschleunigte Pfad (WebProcess 38 nvidia-fds, §17), es ist also kein Software-Rasterizer-Artefakt.

## 2026-09-21 - Weisse Seiten im Lesesaal: pdf.js reicht Bilder als OffscreenCanvas-Bitmap
- Entscheidung: Unter Linux bekommt `getDocument` zusaetzlich `isOffscreenCanvasSupported: false` (`documentOptions` in `PdfReader.tsx`, an der UA erkannt). Andere Engines bleiben auf pdf.js' Default, weil der Fehler dort nicht gemessen ist (§19: macOS rendert dieselben Hefte korrekt).
- Verworfen: `WEBKIT_SKIA_ENABLE_CPU_RENDERING=1` beim Start setzen - behebt es vollstaendig (27/27 statt 15/27), nimmt aber der GESAMTEN Oberflaeche den GPU-Pfad, den §17 sich gerade erst erarbeitet hat. Ebenso `enableHWA`/`willReadFrequently`: pdf.js setzt beides intern schon (`enableHWA` ist per Default aus), und es genuegt nicht.
- Grund: Gemessen an den beiden gecachten Heften (PC World 9506, JPX + JBIG2-Maske je Seite; CGW 1995-07, reines JPEG). In einer WebKit-Harness mit derselben pdf.js-Version und denselben Payloads, neun Seiten, drei Laeufe: PC World 14 von 18 weiss, CGW 12 von 12 weiss - mit dem Flag 0 von 30, und schneller (195 statt 368 ms/Seite). In der App auf dem Produktions-Renderpfad (Wayland/NVIDIA, `WEBKIT_FORCE_DMABUF_RENDERER=1`): ohne Flag PC World 1 von 7 und CGW 4 von 4 weiss, mit Flag 11 von 11 korrekt. Poppler rendert jede dieser Seiten.
- Gotcha: pdf.js meldet dabei NICHTS - kein "Dependent image isn't ready yet", keine Worker-Warnung, die Seite gilt als fertig gerendert; nur ein Pixel-Readback findet es. Welche Seite es trifft, wechselt von Lauf zu Lauf. Und der Fehler ist unter Xvfb/X11 unsichtbar, weil `choose_render_path` dort den disable_dmabuf-Arm nimmt (§17) - Canvas- und GPU-Fehler muessen auf der Wayland-Sitzung gemessen werden, sonst misst man den Software-Rasterizer.

## 2026-09-21 - Lesesaal: Seiten vorrendern und die letzten behalten
- Entscheidung: Der Reader rendert ab 1600 px vor dem Viewport statt 600 px und haelt bis zu sechs gerenderte Seiten (`RENDER_BUDGET`, `evictable`: am laengsten nicht gesehene zuerst, nie eine sichtbare). Die Neu-Render-Grenze beim Zoomen nutzt dieselbe Konstante statt einer zweiten 600.
- Verworfen: Seiten nie freigeben (ein Heft hat mehrere hundert Seiten zu je ~6 MB Bitmap).
- Grund: Report "spaetere Seiten dauern lange, Scrollen laedt nicht sofort". 600 px sind bei einem 600-dpi-Scan (~1400 CSS px hoch) weniger als eine halbe Seite Vorlauf, und jede verlassene Seite wurde sofort genullt und beim Zurueckblaettern komplett neu dekodiert (JPX + JBIG2, einige hundert ms).
- Gotcha: NICHT gemessen - die Wirkung ist aus dem Code abgeleitet, die Laborlaeufe wurden abgebrochen, getestet wird von Hand. Ob spaete Seiten zusaetzlich am Range-Nachladen ueber den Media-Server haengen, ist offen.

## 2026-09-21 - "Open externally" ohne Spur: Exit-Code lesen, AppImage-Umgebung auch hier saeubern
- Entscheidung: `open_document` und `open_log_folder` gehen auf Linux durch `shell_open::open_with_default_app` - eigener `xdg-open`-Spawn mit `sanitize_appimage_env`, 2 s auf einen fruehen Exit gewartet, Exit-Code als Fehlertext (xdg-open(1): 3 = kein Handler, 4 = Handler-Start gescheitert), Ergebnis im Log. Der Viewer zeigt den Fehler samt Dateipfad statt nur `console.error`. macOS/Windows bleiben beim Opener-Plugin.
- Verworfen: nur Logging; das Freedesktop-OpenURI-Portal ueber D-Bus (die saubere Loesung fuer Flatpak-Handler, braucht aber fd-Passing und `zbus` fuer einen Nebenknopf).
- Grund: Issue #30 (Bazzite/KDE, Okular als Flatpak, AppImage 0.14.3): Knopf tut nichts, Log leer. Das Plugin double-forkt `xdg-open` mit `/dev/null` fuer beide Ausgaben und meldet Erfolg, sobald der Zwischenprozess endet - ein sterbender Launcher ist unsichtbar. Ob die vererbte AppImage-Umgebung die Ursache ist, ist NICHT gemessen: auf der Referenzbox (KDE, CachyOS) oeffnete derselbe Knopf den Browser.
- Gotcha: `xdg-open` kehrt je nach Desktop sofort zurueck (KDE: `kde-open`) oder erst, wenn der Viewer schliesst (generischer Pfad) - darum das Zeitfenster statt `wait()`. Ein spaeter Exit wird nur geloggt, sein Code ist der des Viewers, nicht des Starts.

## 2026-09-21 - Storage-Tab: ein Walk zur Zeit, Spieleliste nach dem Walk
- Entscheidung: `load()` ist modulweit und koalesziert - ein Aufruf waehrend eines Walks merkt eine zweite Runde vor statt parallel zu laufen. Die Spieleliste wird NACH dem Ordner-Walk gelesen und nur uebernommen, wenn seit dem Lesen kein Uninstall lief (`listGen`); ein Uninstall nimmt seine Zeile sofort aus der Liste, `performUninstall` meldet dafuer Erfolg als `boolean`.
- Verworfen: nur ein `loading`-Guard (haelt den Zombie nicht auf - der laufende Walk landet trotzdem mit seiner alten Liste); Refetch der Liste pro Uninstall (war die Quelle des Race, nicht die Loesung).
- Grund: Uninstall im Storage-Tab liess sich beliebig oft mit Erfolgs-Toast wiederholen, die Zeile blieb. Log: vier `uninstall_game … not marked installed - cleaning up leftovers anyway` in 2,3 s, danach sieben `storage_overview` von je 100-127 s parallel (allein: 18 s). Der Walk von vor dem Uninstall schrieb seine Momentaufnahme ueber die frische Liste, jeder Uninstall startete einen weiteren Walk.
- Gotcha: Rust antwortet auf einen Uninstall ohne `installed` absichtlich mit Erfolg (Halbzustaende, §5) - der Toast war nie falsch, nur die Zeile.

## 2026-09-23 - Uninstall: Dateien aus dem englischen Archiv sind keine Nutzerdaten
- Entscheidung: `PristineIndex` haelt pro Pfad mehrere (Groesse, CRC)-Eintraege, `merge` ist Vereinigung. Beim Uninstall einer eigenstaendigen LP-Variante wird der Index des englischen Geschwister-Archivs (Zip oder Manifest) dazugelegt; eine Datei, die einem der beiden entspricht, wird geloescht statt nach `!save` gesichert.
- Verworfen: Kontamination beim Restore erkennen (gleiches Index-Problem, und Restore ist die falsche Stelle, Daten zu verwerfen); nichts tun und auf Reset verweisen (Nutzer sieht "DE installiert, EN laeuft" und kommt nie auf Reset).
- Grund: Vor 0.14.1 spielte der Restore das geteilte `!save/<sc>` (Whole-Dir-Backup des englischen Spiels) ueber jede frisch entpackte Uebersetzung. Der Fix von damals stoppt neue Faelle; ein schon verseuchter Ordner blieb es aber, weil jeder Uninstall die englischen Dateien als "geaendert gegen das DE-Archiv" sicherte und jeder Reinstall sie zurueckkopierte. Space Quest III DE lief seit Juli auf Englisch, `diff -rq` Backup vs. Spielordner leer.
- Gotcha: Greift nur, wenn das englische Archiv oder sein Manifest da ist - ein 0-Byte-Platzhalter liefert keinen Index, dann hilft nur Reset. Ein Overlay (§10a) braucht wie bisher BEIDE Indizes, sonst gar keinen.

## 2026-09-24 - Windows: 60 GB Nullen im Pack-Archiv - librqbit setzte das Sparse-Flag nur bei Debug-Logging
- Entscheidung: Fork-Commit `ad57d887` zieht `mark_file_sparse` aus dem `tracing::debug!`-Makro, Fehlschlag wird `warn` mit OS-Fehler; Upstream-PR ikatson/rqbit#681. Exodium migriert bestehende Installationen selbst (`torrent::sparse`, einmal pro Root vor dem Session-Start): volle, nicht-sparse Torrent-Dateien ab 64 MiB werden gelesen, sparse markiert, Null-Bereiche per `FSCTL_SET_ZERO_DATA` freigegeben.
- Verworfen: nicht selektierte Dateien auf 0 kuerzen (die Fastresume-Ledger haelt die gestreamten Tail-Pieces fuer vorhanden und laese danach Nullen; ein importierter eXo-Bestand ist ebenfalls nicht selektiert und voll); Flag nur in Exodium vor dem Stream setzen (repariert `ensure_file_length` nicht und laesst die Ursache im Fork).
- Grund: Storage-Tab zeigte 65,7 GB Pack-Archive bei nie geladenen 60 GB; Log `error writing chunk ... os error 112` fuer DOSMagazines.zip (102 GB Tail-Offset bei 97 GB frei). `fsutil sparse queryflag`: keine librqbit-Datei sparse, derselbe FSCTL aus PowerShell im selben Ordner erfolgreich. Im Windows-Lab: Test ohne Subscriber rot (`attrs=0x20`), mit Fix gruen. Upstream seit PR #390 (2025-06) unbemerkt - rqbits CLI loggt ebenfalls `info`, und ein Voll-Download endet ohnehin voll belegt.
- Gotcha: tracing wertet Event-Felder lazy aus; jeder Seiteneffekt in einem Log-Makro verschwindet im Release. Und Zero-Data auf einen Bereich, der bereits Nullen liest, ist fuer Leser ein No-op - darum darf die Migration jede Datei anfassen, muss aber den Inhalt scannen statt der Auswahl zu trauen.

## 2026-09-24 - Lesesaal: Render-Warteschlange statt Direktstart pro Seite
- Entscheidung: `PdfReader` startet Renders nur noch aus einem rAF-Scheduler (`nextToRender`): hoechstens zwei gleichzeitig, naechste Seite zum Viewport zuerst, bei gleichem Abstand die untere. Eine Seite, die das Beobachtungsfenster verlaesst, wird sofort abgebrochen, wenn sie unfertig ist; fertige bleiben im 6er-Budget. `renderPage` prueft nach jedem `await`, ob die Seite noch gewollt ist.
- Verworfen: Vorlauf verkleinern (600 px gab "Bild kommt nach dem Scrollen", 2026-09-21); `page.cleanup()` nach Abbruch (pdf.js raeumt per `_tryCleanup` selbst).
- Grund: 200 Seiten durchscrollen (Windows, WebView2) liess die Zielseite ~20 s schwarz. Jede vorbeifliegende Seite startete `render()`, pdf.js dekodiert im Worker strikt in Auftragsreihenfolge, und der Abbruch beim Verlassen griff ins Leere: `renderPage` stand noch in `getPage()`, ohne Task, und renderte danach auf die genullte Canvas. NICHT gemessen nach dem Umbau - Handtest auf dem Windows-Rechner steht aus.
- Gotcha: `RenderTask.cancel()` stoppt den Worker erst nach `RENDERING_CANCELLED_TIMEOUT` (100 ms) ueber `_abortOperatorList` → `streamReader.cancel`; ein Abbruch ist also wirksam, aber nicht sofort.
